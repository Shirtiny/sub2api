# Verification — 2026-09-18

## Implementation
- Replaced the default HomeView with a public cafe-style landing page: hero, capabilities, three-step guide, native FAQ disclosures, closing CTA, and footer.
- No dashboard sidebar, subscription offers, account balance, fabricated statistics, announcements, availability badges, or free-credit promises.
- Reused supplied artwork as self-hosted, small frontend assets. Originals and `assets/ui/` remain unchanged.
- Preserved custom HTML/URL homepages, public branding, conditional documentation links, global theme/locale, and role-aware dashboard entry. Authentication restoration remains in the existing router rather than being duplicated on HomeView mount.
- Added only the `home.landing` namespace to each locale; unrelated in-progress changes were preserved.

## Passed
- Scoped ESLint: no errors or warnings on changed frontend files.
- Full `pnpm lint:check`: no errors; 12 warnings in unrelated files.
- `pnpm typecheck`.
- `pnpm test:run src/views/__tests__/HomeView.spec.ts`: 12 passing tests, including guest/user/admin routing, branding, optional/safe links, settings fetching, custom homepage overrides, theme persistence, and Chinese/English content.
- Isolated Vite build to `/tmp/sub2api-home-preview-dist`. The backend's embedded frontend directory was not built over. Existing Browserslist and large-chunk warnings remain.
- Seven real Chromium layouts: 1440px desktop light/dark Chinese, 390px mobile light/dark Chinese, 1440px desktop English, 320px mobile English, and 768px tablet.
- Browser checks: no horizontal overflow, broken images, unresolved translations, personal statistics, or page errors; guest entry, guide anchors, FAQ expansion, theme persistence, and locale switching work.
- Automated axe WCAG 2/2.1 A/AA checks: zero detected violations across those layouts. This is an automated check, not a full accessibility certification.
- Browser traffic used injected sample public settings and intercepted API responses. The only guest API request in these previews was the existing `/setup/status` check; no live backend or private user API was accessed.
- `git diff --check`.

## Full-suite limitation
Full frontend suite: **912 passed, 5 failed** (134 passing files, 4 failing files). The failures are in unchanged functionality:
1. `usePersistedPageSize.spec.ts`: system table default versus stored preference.
2. `EmailOAuthButtons.spec.ts`: affiliate parameter expectation.
3. `HelpTooltip.spec.ts`: hover visibility expectation.
4. `SubscriptionsView.spec.ts`: two date/timezone expectations.

These are the same five failures documented and reproduced against a pristine baseline by the previous task in `../09-17-usage-request-origin/verification.md`. They were not changed as part of this design task.

## Local artifacts
- Screenshots and browser/axe results: `output/home-redesign/` (gitignored).
- Browser test script and temporary test dependencies: `/tmp/sub2api-home-browser/` (not application dependencies).
- Full suite log: `/tmp/sub2api-home-suite-final.log`.
- Build log: `/tmp/sub2api-home-build-final.log`.
- Browser log: `/tmp/sub2api-home-browser.log`.

## Delivery boundary
No commit, push, deployment, production service action, database action, or production setting change. The temporary loopback preview process was stopped after checks. A configured custom homepage or backend-only mode still takes precedence over the default public landing page, as before.

## Follow-up: public preview and configured name
- At the user's request, an isolated static preview is running on TCP 4178, with only that IPv4 ingress port allowed. It has no production API proxy and sends login links to a preview-only notice.
- Removed the header logo image and its unused derived WebP asset. The original supplied logo is unchanged.
- Header/footer names use public `site_name`, then the shared app-store name, with `Sub2API` as the product fallback; no fixed cafe name remains in HomeView.
- Read the existing public settings endpoint once (GET only) to confirm `site_name` is `Café Shop`. The preview uses a local name-only snapshot; no production configuration was changed and no account data was accessed.
- Added three name regression tests (store fallback, asynchronously loaded settings, product fallback) and updated logo assertions: 15 homepage tests in total.

## Follow-up: stronger text-only wordmark
- Enlarged the site name from 23px to 36px on desktop, with bold serif typography and the page's warm accent color; mobile sizes now range from 24px to 28px instead of 18px.
- Balanced the desktop header using symmetric grid columns around the navigation, keeping the site name aligned with the hero content. Tablet/mobile use two columns and preserve space for controls.
- Kept the backend-configured name, no logo image, and title/ellipsis handling for unusually long names.
- Scoped ESLint, typecheck, all 15 homepage tests, and isolated build passed.
- Real browser checks at 1440, 1024, 768, 390, and 320px, including both themes and English: no overflow, overlap, clipped current site name, or browser errors. Long-name layout checks also passed.
- Screenshots and results: `output/home-redesign/wordmark-*`. Updated only the existing temporary preview on 4178; no production app changes.


## Follow-up: artistic rotating wordmark and product icons
- Replaced the heavy header typography with self-hosted Cormorant Garamond 400; the configured site name is italic, Codex/pi use regular serif lettering.
- The configured site name, Codex with its icon, and pi with its icon rotate every 4.2 seconds with a restrained fade/vertical transition. Header geometry stays fixed.
- Codex uses the icon distributed by OpenAI's official IDE extension; pi uses its downloadable site SVG. Both are local assets with provenance in the asset README. The site-name frame has no logo.
- Pointer hover, keyboard focus, an explicit pause button, hidden-document state, and reduced-motion preferences are respected; intervals are cleaned up on unmount.
- Nine new component tests plus fifteen homepage tests passed (24 total). Typecheck and full lint passed before the final image-only change; lint retains 12 unrelated warnings.
- Full suite: 924 passed, the same five unrelated baseline failures remain (four files), recorded in `/tmp/sub2api-home-animated-suite.log`.
- Real Chromium checks of the animated layout passed at 1440px light/dark, 1024px, 390px and 320px: font loaded, no overlap/overflow/current-name clipping, stable dimensions, working pause/reduced motion, no runtime errors, zero detected header axe violations.
- Browser checks abort off-origin traffic. Existing unrelated application imports attempt to initialize payment SDKs; the added font/icon assets do not use third-party runtime requests. No production API is proxied.
- The user-authorized static preview on 4178 was updated, not production. Build output is isolated under `/tmp/` and originals remain unchanged.
- Final icon-only follow-up: isolated build `/tmp/sub2api-home-icon-preview-dist`, scoped lint and all 24 targeted tests passed. Public preview returns HTTP 200 via an external proxy. Chromium verified both icons load at 1440px and 320px with neither clipping nor overflow; screenshots are `output/home-redesign/icons-*.png`.

## Follow-up: continuous SVG geometry morph (replaces the carousel)
- Replaced rotating HTML/text/image frames with one inline SVG path whose coordinates morph from the configured name to the Codex knot, to pi, and back. Icons have no word labels. Removed captions, indicators, bitmap icon and runtime font-face rules.
- Latin name glyphs are precomputed from the licensed local font. The shared/delta-encoded atlas is approximately 44 KB gzip and is bundled with the component, eliminating font-network races. A local regeneration script and asset provenance are included; no new runtime/package dependency was added.
- The 15-second SVG timeline holds each identity for 3.6 seconds and morphs for 1.4 seconds; contour linework softens the transition rather than swapping/fading whole cards. Pi colour arrives only after the geometry has formed.
- Contours preserve negative spaces using even/odd fill; loop counts and point correspondence are compatible across every frame. Name changes rebuild outlines and reset the SVG timeline.
- Hover, focus, explicit pause, background-tab pause, and reduced motion are retained using native SVG timeline controls. There are no JavaScript frame timers.
- Scope: the 229 supplied Latin glyphs support the current Café Shop name and dynamic Latin names. Other scripts retain their actual name as static SVG text with local system fonts; no remote font request or hard-coded name substitution. This fallback deliberately does not morph unsupported names.
- Targeted tests: 24 passed. Typecheck and scoped ESLint passed. Full lint: 0 errors, 12 existing unrelated warnings. Full suite: 924 passed, the same five unrelated failures in four files.
- Chromium SVG checks passed at 1440px in both themes, 390px and 320px, including intermediate shapes. Actual animated path geometry changes, stable link identity, no overlap/overflow, no font requests, no bitmap/visible text nodes in the Latin wordmark, explicit pause, reduced motion, and zero detected header axe violations.
- Artifacts: `output/home-redesign/svg-*.png`, `svg-checks.json`; `/tmp/sub2api-home-svg-*.log`. Final preview build is isolated in `/tmp/sub2api-home-svg-final-preview-dist`. Only the user-authorized temporary preview is updated; production remains unchanged.

- Final SVG build rechecked after contour-line styling: 24 targeted tests, typecheck and scoped lint passed. Public preview returned external HTTP 200, and all four browser/axe/font-request checks passed again against the public preview build.

## Follow-up: stage choreography, slower pacing and corrected Codex identity
- The user identified the previous OpenAI/ChatGPT knot as incorrect. Replaced it with Codex's cloud silhouette and `>_` face, visually verified against the official Codex promotional image. Vendored the Codex SVG from LobeHub commit `63c800e3db6427b3f156f7b88d426ccb3f7277a1` with its MIT notice; did not upgrade the icon package.
- Regeneration now reads the local Codex SVG, so it cannot silently regenerate the old knot. A regression assertion checks the cloud and its two face cutouts. Earlier references to the knot in this historical verification document describe superseded builds, not the current design.
- Extended the SVG cycle from 15 to 24 seconds: each phase has 5.6 seconds for its reveal/hold, then a 2.4-second choreographed transition.
- Distinct reveals: site lettering is traced in glyph order then fills with ink; Codex floats upward with a soft scale/tilt settle and one gentle breath; pi's three coloured modules arrive at staggered times.
- Replaced direct silhouette interpolation with authored thread, orbit, modular-grid and pen-loop intermediates. Solid ink drains before coordinates move; morphing is rendered in fine linework, not overlapping filled blobs. All effects share the same native SVG clock; hover/pause, hidden-tab state and reduced motion affect them together.
- No runtime fonts, bitmap wordmarks, animation libraries, new environment variables or JavaScript animation timers.
- Targeted tests now total 25 and pass. Typecheck and scoped ESLint pass. Full lint has 0 errors and the same 12 unrelated warnings. Full suite before the final cloud-asset/visual-timing correction: 925 passed, the same five baseline failures in four files.
- Browser checks on the corrected cloud build passed in light/dark desktop and 390/320px mobile: 14 timeline samples per layout, correct cloud shape, distinct reveals, pi settlement, no solid fill during transition intermediates, no clipping/overflow, no font requests, working pause/reduced motion, and zero detected header axe violations.
- Visual contact sheet: `output/home-redesign/choreography-contact.png`; timeline metrics: `choreography-checks.json`. Source attribution is in `frontend/src/assets/home/README.md`.
- Delivery remains the isolated, user-authorized static preview on port 4178. No production service, database, settings, commit or push changes.
- Final cloud/choreography preview (`/tmp/sub2api-home-cloud-final-preview-dist`) returned external HTTP 200; all four browser/axe/timeline checks passed again against the served build. The final seam-continuity assertion passed, and vector regeneration reproduced identical hashes. The loop's handwriting overlay is invisible while its dash resets, avoiding an endpoint flash.

## Follow-up: simplified coffee-ink splash instead of shape distortion
- Replaced forced path interpolation and all ring/grid/ribbon/pen-loop intermediary geometry with stable identity outlines and a small coffee-coloured splash. Removed contour sorting, correspondence/alignment and intermediate-ring generation.
- The outgoing fill drains into a thin trailing outline, followed by three short asymmetric brush arcs and four tiny liquid drops; the next outline is traced and filled. Silhouettes never deform and there are no animated `d` attributes.
- Kept the correct Codex cloud/terminal-face icon, pi colours, backend-configured site name, distinct phase reveals and slower 24-second identity cycle. The splash repeats every eight seconds on the same native SVG clock.
- Full reduced-motion mode renders only a solid site-name outline, with no splash or animation elements. Hover, explicit pause, keyboard focus, hidden tabs and live name changes still control all animation layers together.
- Typecheck and all 25 targeted tests passed. Full lint: 0 errors and the same 12 unrelated warnings. Full suite: 925 passed and the same five unrelated baseline failures in four files.
- Chromium checks passed for desktop light/dark and 390/320px mobile, with 16 timeline samples each: stable outline lengths, only the appropriate identity visible during holds, splash-only midpoint, bounded droplets, no layout overflow/overlap, no font/image dependencies, working SVG pause/reduced motion, and zero detected header axe violations.
- Screenshots/contact sheet: `output/home-redesign/splash-*.png`; metrics: `splash-checks.json`; logs: `/tmp/sub2api-home-splash-*.log`.
- Isolated build `/tmp/sub2api-home-splash-preview-dist`; only the existing user-authorized temporary preview on TCP 4178 was updated. Production services, settings and databases remain unchanged.
- Served preview verification: external HTTP 200 and all four browser/timeline/axe checks passed again against port 4178 after the preview-only update.

## Follow-up: attached ink transfers (no inserted splash cutaway)
- The user correctly noted that the previous standalone arcs/drops had no geometric relationship to the next identity. Those decorative overlays are removed.
- Split real source/destination SVG contours into arc-length-sampled fragments. For the current Café Shop name there are 40 fragments per transfer. The same fragment paths detach from the source, contract into rounded liquid strokes, travel on coherent upward curves, then expand onto the destination contour.
- Arrival fragments collectively form the next outline BEFORE its ink fill appears. The return transfer derives its target from the configured site name, not a fixed wordmark.
- Kept gentle 24-second timing, correct Codex cloud, pi colours, inline vectors, reduced motion and native SVG pause/visibility handling. Delayed Codex float until after arrival and removed pi's geometric landing offsets so show effects cannot pull the target away from arriving fragments.
- Added a geometry regression test: every transfer's arrival fragments equal the next transfer's departure fragments (allowing reversed traversal), all 13 intermediate samples use compatible path commands and remain in the SVG bounds, and renaming the site updates both connected transfers.
- Typecheck and 26 targeted tests passed. Full lint: 0 errors, 12 unrelated warnings. Full suite before the final stroke-count/curve refinement: 926 passed, the same five unrelated baseline failures in four files. Targeted tests and scoped lint passed again after refinement.
- First browser pass checked all three departure and arrival boundaries against the actual SVG outlines, not just screenshots: nearest-contour distances were subpixel. It also verified continuous visible ink in transit, identity holds, containment, no font/image dependency, explicit pause/reduced motion and zero detected header axe violations in desktop light/dark and 390/320px layouts.
- Visual/timeline artifacts: `output/home-redesign/flow-contact.png`, `flow-*.png`, `flow-checks.json`; logs `/tmp/sub2api-home-flow-*.log`. Output remains an isolated `/tmp/` preview build; no production action, database/settings modification, commit or push.

- Final served build `/tmp/sub2api-home-flow-final-preview-dist`: external HTTP 200; all four browser/axe/timeline checks passed after the temporary-preview update. Maximum measured departure/arrival contour error was 0.262 SVG units across all tested endpoints (subpixel in the desktop layout). No runtime errors or font requests.

## 2026-09-19 — accepted quiet-wordmark direction
- At the user's approval, removed all rotation/morph/ink-flow effects, animation timelines, pause controls and timer/visibility state. The primary identity is now always the backend-configured site name.
- Rebuilt the header as a light, left-aligned SVG wordmark with slightly open letter spacing, a short warm rule and a small localized annotation. Other scripts remain static SVG text using local system fonts. Live name updates retain the same SVG node and do not replay the entrance.
- Only two short, one-shot CSS entrances remain: the sign settles over 650 ms; the rule extends over 800 ms. Both finish and stay still. Reduced motion disables both immediately.
- Moved the correct Codex cloud/terminal-face icon and original pi icon to a small, static tool-reference row BELOW the hero. Both appear together with readable labels, not as substitutes for the site's identity.
- Deleted the retired `brandMotion.ts`, fragment-transfer code and unused motion locale strings. Reduced `brandGeometry.ts` to a wordmark-only builder and regenerated a glyph-only atlas. No runtime font request, dependency addition or environment variable.
- Typecheck, isolated Vite build and 22 targeted tests passed. Full lint: 0 errors, 12 existing unrelated warnings. Full suite: 922 passed and the same five known baseline failures in four files; this run additionally observed an unhandled `requestAnimationFrame is not defined` teardown error in unchanged `KeyUsageView.vue:574` / `KeyUsageView.spec.ts`. No unrelated fix was made.
- Browser checks passed at 1440px in both themes, 768px, 390px and 320px: full current name/annotation fit, no overflow/overlap, only one-shot completed animations, no SVG animation elements/controls, unchanged wordmark after settling, both tool icons load BELOW the hero, no font requests and no runtime errors. Full-homepage automated axe WCAG 2/2.1 A/AA checks found zero violations across all layouts.
- Screenshots/results: `output/home-redesign/quiet-*.png`, `quiet-checks.json`. Logs: `/tmp/sub2api-home-quiet-*.log`. Build output isolated in `/tmp/sub2api-home-quiet-preview-dist`.
- Updated only the existing user-authorized temporary static preview on 4178; no production application/service, settings, database, commit or push changes.
- Isolated rerun of unchanged `KeyUsageView.spec.ts`: 1/1 passed without the teardown error (`/tmp/sub2api-home-quiet-keyusage-recheck.log`). The full-suite observation above is retained rather than reporting the full suite as clean.
- Final served-preview verification: external HTTP 200; the five browser/layout/reduced-motion/full-page axe checks passed again against port 4178 after the preview-only update.

## Follow-up: broader upright lettering with restrained tool rotation
- Replaced the narrow italic source with upright Lora regular 400, drawing the backend-configured name as local SVG outlines. The current desktop Café Shop lettering measures about 210 px wide versus about 160 px previously, without synthetic stretching or bold weight. The original, unmodified font and OFL notice are vendored for regeneration only; removed the unused former font source/notice. The 223-glyph atlas is about 37 KB gzip; no runtime font request or dependency addition.
- Kept the site name stable. Added a separate Codex/pi mark beside it, using the existing correct cloud/terminal-face Codex SVG and original pi SVG. A CSS-only 12-second cycle rests 4.8 seconds per mark and overlaps a 1.2-second fade/lift. No particles, fragments, font switching, morphing or JavaScript animation timers.
- The mark is an accessible pause/resume button outside the home link. Keyboard activation, hidden-tab pause, manual pause persistence, and reduced motion are covered. Reduced motion shows one static mark. On narrow phones the mark moves to the annotation row rather than narrowing the name. The existing static tool references below the hero remain unchanged.
- Typecheck and isolated build passed. Targeted tests: 25/25 passed. Full lint: 0 errors, 12 unchanged warnings. Full suite: 925 passed, the same five known unrelated failures in four files (`usePersistedPageSize`, `EmailOAuthButtons`, `HelpTooltip`, two `SubscriptionsView` date assertions); no unhandled teardown error in this run. Final targeted tests passed again after source-font regeneration; final scoped lint was rerun separately after the previous combined shell was terminated following successful tests.
- Chromium checks cover 1440px light/dark, 1024px, 768px, 480px, 390px, and 320px: no overlap/overflow/caption clipping; stable name; both correct local SVGs load and alternate; overlapping transitions never blank out; click/Space pause-resume and reduced motion work; no font requests or runtime errors; full-homepage axe WCAG 2/2.1 A/AA checks find zero violations. Visual review also caught and corrected a scoped-CSS dark selector before updating the public preview; document-wide filters are now explicitly checked.
- Artifacts: `output/home-redesign/lora-*.png`, `lora-checks.json`; `/tmp/sub2api-home-lora-*.log`. Final build: `/tmp/sub2api-home-lora-final-preview-dist`.
- Updated only the existing user-authorized read-only temporary preview on port 4178. Local and external HTTP 200 confirmed. No production service/settings/database, commit, push, or unrelated source changes.
- Final served-preview recheck: all seven layout/animation/axe checks passed on port 4178; separately observed a real-time 12-second Codex → pi → Codex cycle without seeking, with the configured wordmark unchanged. Final scoped ESLint exited successfully.

## Follow-up: requested CafeCode Imagegen design alternatives (authentication blocked)
- User rejected the implemented lettering and requested generated concept images before further code changes. Read the `cafecode-imagegen` skill and prepared four distinct directions in `output/home-redesign/concepts-20260919/prompts/`, grounded in the existing banner and correct Codex/pi reference marks.
- Dry-run confirmed the reference edit endpoint, gpt-image-2, 1536x1024 PNG, high quality and two references. All actual requests returned HTTP 401 `INVALID_API_KEY`; no images were generated. The configured credential exists but was rejected; no CafeCode-specific environment fallback is present. No unrelated credentials were tried or exposed.
- Need the user to update `/root/.codex/config.toml` field `cafecode-imagegen-key` before retrying. No application source, homepage preview, production, or API configuration changes. Prepared gallery script is only a local scratch file and has not published anything.

## Follow-up: remove the caption and animate SVG paths, not image cards
- Removed the line, caption and their locale strings below the configured wordmark. Removed the circular tool-icon frame. Kept the broad Lora outlines and backend-defined site identity unchanged.
- Replaced the image crossfade with `HomeToolGlyph.vue`: one native SVG clock animates actual cloud outlines, terminal-face strokes, ink fill, and pi's three original color paths. The Codex cloud traces and fills; it returns to linework before pi traces/assembles with a 140 ms tile stagger. The complete marks each hold roughly four seconds in a 16-second loop. There are no particle/morph fragments, image elements, CSS whole-card swaps or JavaScript frame timers in the header tool stage.
- `toolArtwork.ts` holds the exact vendored Codex compound path and pi paths/colors. Regression tests compare them to the original SVG files, including the Codex cloud's two negative-space cutouts. The outer cloud contour comes from the original first subpath; terminal sketch strokes are replaced by the exact compound mark when filled.
- Preserved independent click/keyboard pause, manual pause persistence and hidden-tab pause through native `pauseAnimations` / `unpauseAnimations`. Reduced motion removes animation nodes entirely and renders one complete static mark rather than freezing incomplete geometry. Dynamic site-name updates do not replace the tool SVG or restart its clock.
- The tool stays beside the wordmark at every size; below 360 px only the header actions wrap so the configured name does not get squeezed. No subtitle/annotation row remains.
- Typecheck, isolated Vite build and 29 targeted tests passed. Full lint: 0 errors, the same 12 unrelated warnings. Full suite: 929 passed, five known baseline failures in four files; no unrelated changes to address those failures. Final targeted tests passed again after adding the stable-tool-node assertion.
- Chromium checks passed at 1440px light/dark, 1024px, 768px, 480px, 390px and 320px. Verified removal of subtitle/frame/images, stable wide name, no overlap/overflow, changing SVG stroke/fill values, staggered tile transforms, full Codex/pi phases, native-clock pause/resume, background pause, reduced motion, zero font requests/runtime errors and zero automated full-homepage axe WCAG 2/2.1 A/AA violations. Inspected intermediate-frame contact sheet, desktop/dark/mobile screenshots.
- Artifacts: `output/home-redesign/ink-*.png`, `ink-checks.json`; `/tmp/sub2api-home-ink-*.log`; isolated build `/tmp/sub2api-home-ink-preview-dist`.
- Updated only the existing user-authorized read-only temporary preview on port 4178. Local and external HTTP 200 confirmed. No production service/settings/database changes, commit or push.
- Final served-preview verification: all seven browser/axe layouts passed again against port 4178. A separate real-time check observed Codex → pi → Codex without seeking and with the name unchanged. An initial reduced-motion visit had a complete mark and no animation nodes. Final scoped lint passed (rerun with the correct relative paths after a command-path typo).

## Follow-up: smaller upper-right SVG tool mark
- Moved the existing animated Codex/pi stage to a superscript-like position at the configured wordmark's upper-right. Reduced the desktop control from 52 px to 32 px (visible artwork about 20 px), and mobile to 28 px. Raised it by 10 px desktop / 7 px mobile, without overlapping the home link or changing SVG timing, paths or accessibility controls.
- Bound layout width to the computed wordmark width so the mark follows the end of the configured name, not a fixed empty column. Preserved the wide lettering and clean caption-free design. Extended dynamic-name tests to cover the width binding.
- 29 targeted tests, scoped ESLint, typecheck and isolated build passed. No broad-suite rerun for this positioning-only follow-up; the prior native-SVG change's full-suite baseline is recorded above.
- Eight Chromium layouts passed: 1440px light/dark, 1024px, 768px, 480px, 390px, 360px and 320px. Checked smaller control dimensions, raised upper-right alignment, no name/header overlap or overflow, native animation phases, pause/keyboard/background/reduced-motion behavior, no font requests/runtime errors and zero detected full-page axe violations. Visually inspected both finished marks beside the name.
- Artifacts: `output/home-redesign/corner-*.png`, `corner-checks.json`; `/tmp/sub2api-home-corner-*.log`; build isolated at `/tmp/sub2api-home-corner-preview-dist`.
- Updated only the user-authorized temporary static preview on 4178; external HTTP 200. No production, database/settings, commit or push changes.
- Final served-preview verification: all eight layout/native-animation/axe checks passed again on port 4178. Mobile header crops also visually confirmed the reduced upper-right mark.

## Follow-up: letter-anchored steam → Codex → pi → liquid story
- Implemented the user's requested narrative inside the backend-defined wordmark rather than a separate corner carousel. The real é accent extends into a steam ribbon, closes into a small Codex cloud, and develops the original terminal-face cutouts. The sampled cloud outline is generated from the existing correct vendored Codex SVG, not a generic cloud or OpenAI knot.
- Refactored `brandGeometry.ts` to retain per-letter contours/bounds without changing the resting wordmark. `brandStory.ts` derives the accent, S/o impact points, and p counter directly from that geometry. Three sampled parabolic arcs, brief cloud squash/rotation, and baseline-anchored letter compression give the impacts weight. The final accelerated drop enters the real p counter; the cloud is drawn behind the letters for rim occlusion.
- The p's original outer/inner rings continuously morph into pi's P-like silhouette; its extra leg grows from the counter. Everything in this header story uses currentColor, including the fallback tool icon. The extra leg becomes a rounded fluid neck and falling drop, fades out, and the original p and é accent restore exactly before the 22-second loop repeats.
- New `HomeBrandStory.vue` uses one native SVG clock, no JavaScript animation/frame/interval timers or added runtime dependencies. Equal-size aligned contour samples keep path morph commands compatible. The stage reserves upper/lower movement space, avoiding layout changes during the cycle. Changed eligible backend names recalculate targets and restart the clock; names lacking the required glyphs retain their actual identity with the prior small corner SVG drawing as a monochrome fallback.
- Kept accessible home-link identity and separate keyboard/pointer pause control, revealed on hover/focus and kept visible when paused. Background tabs pause without overriding manual pause. Reduced motion removes all SVG animation nodes and restores the full static configured name rather than freezing a transformed p or floating cloud.
- The initial sandbox restrictions prevented browser/network access; no preview process was stopped under those restrictions. An actual component SSR export was rendered offline at sampled timeline positions for an initial visual review. After permissions changed, real Chromium checks replaced that provisional review. Test-only issues (read-only SVGTransformList access and an opacity expectation during the deliberate formation crossfade) were corrected in the browser harness, not hidden as application successes.
- Typecheck, scoped ESLint, isolated Vite build and 36 targeted tests passed. Full lint: 0 errors, 12 unchanged warnings. The completed full suite before final liquid-shape/test refinements had 935 passes and the same five unrelated baseline failures in four files; no unrelated fixes. Targeted tests passed again after the final changes.
- Eight browser layouts passed: 1440px light/dark, 1024px, 768px, 480px, 390px, 360px, 320px. Verified actual native animated contour changes, collision coordinates within 0.1 SVG units of S/o/p targets, impact scaling, monochrome pi, runoff, exact p/accent restoration at the loop boundary and second-cycle continuity. Also checked stable stage dimensions, no header overlap/overflow, no runtime errors/font requests, pause/Space/background/reduced-motion behavior, and zero detected full-homepage axe WCAG 2/2.1 A/AA violations.
- Artifacts: `output/home-redesign/story-*.png`, `story-checks.json`, `story-real-frames.json`; logs `/tmp/sub2api-home-story-*.log`. Final isolated build: `/tmp/sub2api-home-story-final-preview-dist`.
- Updated only the existing user-authorized temporary static preview on port 4178, from the corner build to the story build. Local and external HTTP 200 confirmed. No production services, database/settings, commit or push changes.
- Final served-preview checks: all eight browser/axe layouts passed again on port 4178. A separate 24-second real-time observation (no intermediate seeking) confirmed cloud formation, pi, exact name restoration and the next steam cycle. An initial reduced-motion visit rendered the complete static name without animation nodes.

## 2026-09-20 follow-up: permanent steam, separate cloud condensation and rotating release
- Replaced the initial/final acute accent with a permanent steam ribbon. It remains above the e throughout the cycle and also in reduced motion. Its start/end contours match; the original dot/accent no longer reappears. Gentle same-direction curvature changes avoid collapsing the wisp into a straight leaf during interpolation.
- Split the former steam-to-icon morph into distinct actors: a wider, softer, faceless cloud slowly gathers above the steam, then the small Codex appears inside it and drops out while growing to full size and rotating (-10° → 12° → 28° before settling into the first impact). The cloud lingers and thins after release instead of being replaced wholesale by the icon.
- Raised the birth point and replaced the initial upward arc with a downward-accelerating parabolic release. S/o impact times, subsequent bounces, p/monochrome-pi transformation, runoff and the 22-second overall cycle remain unchanged.
- Updated regression coverage for steam on the first/last frames, reduced-motion steam, independent condensation before icon birth, cloud persistence during release, monotonic downward acceleration and early rotation. 37 targeted tests passed; typecheck and full lint passed (0 errors, 12 existing warnings). Full suite: 937 passed, the same five unrelated failures in four files. No unrelated fixes.
- Current managed sandbox blocks local sockets and Chromium startup. Actual Vue-component SSR output was inspected with the existing offline librsvg/Pillow timeline renderer; updated contact sheet: `output/home-redesign/cloud-contact.png`, standalone animation: `cloud-story.svg`. This is NOT a real-browser verification. Browser harness for the updated choreography is prepared at `/tmp/sub2api-home-browser/cloud-check.cjs` for a later allowed run.
- Isolated final build target: `/tmp/sub2api-home-cloud-final-preview-dist`; logs `/tmp/sub2api-home-cloud-*.log`. No preview update has been performed: the existing public preview script still points to `/tmp/sub2api-home-story-final-preview-dist`. Did not stop/restart any preview or production process, modify served assets, settings or databases, commit or push.

### 2026-09-20 continuation: browser verification and preview refresh
- After the environment permissions changed, checked the prepared cloud build in real Chromium at 1440px light/dark, 1024px, 768px, 480px, 390px, 360px and 320px. All eight layouts passed: permanent steam, cloud-before-Codex sequencing, cloud lingering during release, preserved S/o/p target coordinates and impact scaling, monochrome pi/runoff, exact p restoration and steam-loop continuity, no layout shifts/overlap/overflow, no font requests/runtime errors, native pause/keyboard/background/reduced-motion behavior and zero automated full-page axe violations.
- Visually reviewed the rendered page. Updated only the existing user-authorized temporary preview on port 4178, after confirming the PID belongs to `/tmp/sub2api-home-public-preview.py`. It now serves `/tmp/sub2api-home-cloud-final-preview-dist`. Local and external HTTP 200 confirmed. The previous sandbox limitation is resolved for this continuation; no production services/settings/databases or unrelated code changes, commit or push.
- Served-preview recheck: all eight browser/axe checks passed again against port 4178. A separate real-time full-loop observation, without intermediate seeking, confirmed steam → cloud condensation → rotating Codex release → pi → restored p → next cycle. Initial reduced-motion loading showed the complete name with the static steam accent. Screenshots/results: `cloud-live-*.png`, `cloud-live-checks.json`, `cloud-checks.json`; logs `/tmp/sub2api-home-cloud-{browser,public-browser,live-browser}.log`.

## 2026-09-20 follow-up: lively rising steam and a drawn cloud
- Responded to feedback that the steam had lost its movement and the cloud looked poor. Replaced the slowly rocking single ribbon with two tapered traveling waves, offset by half a cycle, with independent opacity modulation. Their 4.4-second native timelines fit exactly five times into the 22-second story, retaining one pausable SVG clock and seamless loop endpoints. Steam is still present from the first frame and remains a static accent under reduced motion.
- Replaced the flattened Codex-derived blob with an asymmetric, hand-drawn cloud outline and a very light interior. Two rising curved stroke segments follow the steam into the actual cloud lobes; the outline gathers progressively, lingers through Codex's release, then dissipates. Kept the accepted Codex size/rotation/trajectories, S/o impacts, p/pi transformation and liquid drain unchanged. Removed the now-unused sampled Codex-contour asset and its generator branch; the exact original Codex icon remains unchanged.
- 38 targeted homepage/branding tests pass; typecheck and full lint pass (0 errors, 12 existing warnings). Isolated build passes at `/tmp/sub2api-home-vapor-final-preview-dist`. No full-suite rerun for this focused visual change. Regressions cover offset steam cycles, compatible contours, anchored roots/tapering, cloud drawing, native clocks, loop restoration and reduced motion.
- Real Chromium checks passed at 1440px light/dark, 1024px, 768px, 480px, 390px, 360px and 320px: visible steam changes within 350ms, cloud-before-Codex sequencing, exact collision targets, preserved pi/runoff, loop continuity, stable layout/no overflow, pause/keyboard/background/reduced-motion behavior, no runtime errors/font requests, and zero automated full-page axe violations. Visually inspected cloud development, actual-size header, desktop and dark mobile renders.
- Updated only the existing authorized temporary preview on port 4178, after confirming its PID/script identity. It now serves the isolated vapor build; local and externally routed HTTP 200 confirmed. No production services, settings or databases changed; no commit or push. Artifacts: `output/home-redesign/vapor-*.png`, `vapor-checks.json`, `vapor-real-frames.json`; logs `/tmp/sub2api-home-vapor-*.log`.
- Served-preview verification also passed all eight layouts on port 4178. A separate real-time full-loop check (without intermediate seeking) passed steam persistence, cloud formation, rotating release, pi, p restoration and the next cycle; an initial reduced-motion visit showed the complete static name. Results: `vapor-live-checks.json`; logs `vapor-public-browser` and `vapor-live-browser` under the prefix above.

## 2026-09-20 follow-up: restore the original steam, not another redesign
- User preferred the first steam shape/motion over both follow-ups. Recovered the accepted implementation from the isolated original preview's `HomeView-wcKZehda.js`, including the original ribbon helper, contour alignment and 0.095–0.15 keyframe interval. Restored that single, fuller wisp rather than inventing another pair of oscillating strands. Removed the later thin branching curls as well.
- Retained the requested steam on the initial frame instead of reinstating the acute accent. The original two steam contours interpolate over the same 2.09–3.30 seconds; the higher puff then rises/dissipates into the existing cloud while the quiet lower steam accent reappears. The invisible puff resets before the end-of-cycle crossfade, avoiding a falling-back/reset artifact. Preserved the cloud outline and all Codex release/rotation, collision, pi and liquid timelines.
- Added a regression fixture extracted from the original built preview rather than regenerated from the new implementation. Both recovered contours match exactly; tests lock their timing and invisible reset. All 38 targeted tests pass. Typecheck, isolated final Vite build and lint pass (0 errors, 12 existing warnings). Full suite: 938 passed, the same five unrelated failures across usePersistedPageSize, EmailOAuthButtons, HelpTooltip and SubscriptionsView.
- Eight real Chromium layouts passed: desktop light/dark, 1024/768/480/390/360/320px. Browser checks additionally compare the actual animated path against the original poses and interpolated midpoint within 0.01 SVG units. Collision targets, pi/runoff, loop continuity, static reduced motion, pause/keyboard/background behavior, stable layout and automated full-page axe checks all pass; no runtime errors or font requests. Visually reviewed original-shaped steam and its cloud handoff.
- Updated only the existing authorized temporary static preview on 4178 after confirming its process identity. New isolated root: `/tmp/sub2api-home-original-steam-final-preview-dist`; local and external HTTP 200. No production services, settings, databases, commit or push changes. Artifacts/logs use the `original-steam-` / `/tmp/sub2api-home-original-steam-` prefixes.
- Final served-preview real-time observation passed the entire 22-second cycle and next-cycle restart without intermediate seeking; a fresh reduced-motion visit rendered the exact complete static wordmark with its steam accent. Results: `original-steam-live-checks.json`, `/tmp/sub2api-home-original-steam-live-browser.log`.

### 2026-09-21 — Preview navigation rendering fix
- Removed-navigation preview branch incorrectly returned raw `null` inside optimized Vue children; reproduced the `Cannot read properties of null (reading 'el')` error with Vue and JSDOM.
- Corrected both desktop/mobile preview branches to Vue comment VNodes. Reproduction now mounts successfully; preview JS passes `node --check`.
- Source uses normal `v-if` and its isolated Vite build passed at `/tmp/sub2api-home-navigation-fix-dist`; homepage tests previously passed (18).
- Production untouched. Direct HTTP and Chromium verification unavailable under this session's sandbox restrictions; server log showed HTTP 200 for HTML/assets during reported failure, consistent with client-side rendering failure.

### 2026-09-21 — Always-visible usage and pricing panels
- Replaced collapsed FAQ rows with semantic, always-visible articles: prominent enforcement notice, Codex/Pi client panel, and separate subscription/balance pricing columns.
- Highlighted no-refund bans, one-month presales and Astra 0.4–0.5×; retained all user-provided restrictions and billing rules. Added English equivalents, dark-theme styles and stacked mobile layouts.
- Homepage tests: 19 passed. Scoped ESLint, typecheck, isolated Vite build and diff whitespace checks passed.
- Refreshed ONLY static preview assets from the complete build in `/tmp/sub2api-home-policy-panels-dist`. Existing cached HTML entry URL now imports the new compiler-generated entry; no manual Vue render-function edits. Relative module paths and entry syntax verified locally. Backup in `/tmp/sub2api-home-pre-policy-panels-backup`.
- Browser/external HTTP verification remains unavailable due to sandbox restrictions. No production service, backend, database, or proxy changes.

### 2026-09-21 — Official client links
- Codex and Pi client cards now link to `https://openai.com/codex/` and `https://pi.dev/`, with external-link icons, hover/focus styling and `target="_blank" rel="noopener noreferrer"`.
- Updated tests to distinguish configured docs links from fixed official client links. Homepage tests: 20 passed; scoped ESLint, typecheck, isolated build and whitespace checks passed.
- Refreshed only the temporary preview from `/tmp/sub2api-home-client-links-dist` via its stable entry bridge. Import paths verified; backup at `/tmp/sub2api-home-pre-client-links-backup`. Network/browser checks unavailable in current sandbox (web tool also denied access). No production changes.

### 2026-09-21 — Model brand gallery
- Added `HomeModelShowcase` directly before the everyday features. Four local monochrome SVG marks: ChatGPT, Claude, Grok, Gemini. Only ChatGPT is marked available; the other three explicitly say unavailable, including English locale.
- Uses subtle distinct logo motion, a breathing warm glow and slow SVG orbit on ChatGPT. Visible pause control; reduced-motion, offscreen and hidden-tab animation suspension. Four desktop columns / two mobile columns; existing light/dark palette.
- Extracted 4.4 KB of path data from installed LobeHub icons 4.0.2, documented MIT provenance; no new dependencies, requests or fonts.
- Passed 46 homepage/component tests, scoped lint, typecheck, isolated Vite build and whitespace checks. Refreshed only the static preview from `/tmp/sub2api-home-model-gallery-dist`; cached HTML uses the existing entry bridge and all relative module imports resolve. Backup: `/tmp/sub2api-home-pre-model-gallery-backup`.
- External HTTP/Chromium visual checks remain restricted by sandbox. Production unchanged.

### 2026-09-21 — Single ChatGPT presentation, Aether-inspired
- Replaced the four-brand cards with a large ChatGPT mark and clean wordmark; removed the English collection heading, description, availability badges, other brands, orbit and pause button. Retained a small supported-models label.
- Located local `/opt/stacks/aether/frontend/src/views/public/Home.vue` and observed its large-logo outline/fill/silhouette-ripple design. Implemented the visual idea independently using the existing MIT-sourced mark; no Aether code copied or modified. User was asked for a URL in case a different Aether was intended.
- Motion is a finite 4.8-second first-entry outline/fill/two-echo reveal, then static; reduced-motion renders the complete mark, with offscreen/background pause.
- Passed 46 related tests, scoped ESLint, typecheck, isolated Vite build and whitespace checks. Preview refreshed from `/tmp/sub2api-home-chatgpt-showcase-dist`, backup at `/tmp/sub2api-home-pre-chatgpt-showcase-backup`; module imports and bridge syntax verified. Browser/HTTP checks still sandbox-restricted. Production untouched.

### 2026-09-21 — Titled ChatGPT model-group sequence
- Restored a full-sized persistent section heading above the brand stage. The ChatGPT outline/fill/echo intro contracts into a small brand header; Luna, Terra, Sol and Asra then appear in a staggered four-column desktop / two-column mobile list.
- Names follow this message's spelling (Asra); asked user to clarify Asra versus previously used Astra. No group specs, prices, access links or unsupported claims added.
- Sequence ends within 4.8 seconds and remains on the group display; reduced motion renders final layout directly, background/offscreen pausing preserved, no playback controls.
- Passed 46 related tests, scoped lint, typecheck, isolated build and whitespace checks. Refreshed ONLY temporary preview via `/tmp/sub2api-home-model-groups-dist`; module imports and bridge syntax verified. Backup: `/tmp/sub2api-home-pre-model-groups-backup`. Browser/HTTP checks unavailable under current sandbox; production untouched.

### 2026-09-21 — Official visual research and independent celestial scenes
- After network was enabled, queried and actually fetched official OpenAI API model pages for GPT-5.6 Luna/Terra/Sol and GPT-6 Astra. Inspected their official 128px PNG artwork: lunar surface, Earth, solar surface and spiral galaxy. Corrected Asra to Astra. Launch/marketing pages remained behind browser verification; this implementation is explicitly independent, not the official site's original animation.
- Replaced symbolic SVG icons with local Canvas astronomical scenes: mapped Moon surface with dynamic illumination, spinning Earth with atmospheric rim, solar texture with moving brightness/prominences, and 1,700 procedural spiral stars. ChatGPT introduction/section heading retained. No model calls, CDN requests or backend changes.
- Three surface maps from Solar System Scope/INOVE: license page successfully fetched and confirmed CC BY 4.0; maps resized to 1024x512 WebP (~299 KB total), modified by custom lighting/animation. Attribution and license links shown under the models. Provenance in assets/home/README.md.
- Rendering throttled to 20fps, stopped offscreen/background, static for reduced-motion. No separate playback button added.
- New lifecycle/model tests: 7 passed. Homepage/model suite: 24 passed, one unrelated existing header story assertion fails because concurrent header work replaced impact letters with umbrella choreography; left that implementation/assertion untouched and excluded only that test on focused rerun. Scoped ESLint, typecheck, isolated Vite build and diff whitespace checks passed.
- Real Chromium checks passed: four independently changing canvas frames, 1440/768/390/320px no overflow, offscreen frames frozen, dark mode, reduced-motion frames frozen, no playback controls or page errors. Screenshots in output/home-redesign/celestial-{desktop,mobile,dark}.png.
- Temporary static preview refreshed from /tmp/sub2api-home-celestial-models-dist. Another session refreshed/restarted that preview concurrently, removing the legacy entry alias; adapted this session's asset publisher to the currently served HTML entry instead of assuming its old filename. Did not stop/restart any process or modify production. Final backup: /tmp/sub2api-home-pre-celestial-final-backup.

### 2026-09-21 — Stylized SVG models and visible size hierarchy
- Replaced photorealistic Canvas scenes with independently authored inline SVG. Removed the canvas renderer, three raster surface maps, image loading/RAF loop, and now-unneeded texture attribution.
- On the same 320×280 viewBox, Luna/Terra/Sol body radii are 26/48/80: deliberately small/medium/large illustrative sizes, not an astronomical scale. Luna animates its phase and orbital point; Terra moves simplified landforms through a clipped globe; Sol rotates rays and flowing curves; Astra rotates spiral lines with a pulsing core.
- Section heading, ChatGPT intro-to-model sequence and model names unchanged. Offscreen/background pause and reduced-motion still supported, without playback controls.
- Focused tests: 31 passed, one previously stale unrelated header choreography assertion excluded. Scoped lint, typecheck, isolated build and diff checks passed. Browser verified SVG-only output (no canvas/image), exact size ratios, independently changing transforms for all four scenes, four viewport widths without overflow, offscreen pause, dark mode and reduced-motion; zero page errors.
- Screenshots: output/home-redesign/svg-models-{desktop,mobile,dark}.png. Updated ONLY temporary preview from /tmp/sub2api-home-svg-models-dist using its current cached entry bridge; backup /tmp/sub2api-home-pre-svg-models-backup. Public preview HTTP 200. Production untouched.

### 2026-09-21 — Official-reference SVG redraw
- User confirmed using official PNG artwork as a reference while retaining pure SVG. Replaced generic diagram rings/meridians/radial icon rays with a left-weighted planet composition and sparse star field: gray lunar mare shapes and terminator, blue Earth/land/cloud layers, gold-orange solar filaments/prominences, and a 360-star spiral galaxy.
- No official PNG embedded; no raster/image/canvas elements, texture downloads, rendering timers or new dependencies. Luna/Terra/Sol radii remain 26/48/80 on the same viewBox. All four have distinct SVG/CSS motion and preserve reduced-motion/offscreen pause.
- 12 focused component tests passed; scoped lint, typecheck, isolated build and whitespace checks passed. Chromium validated actual motion for all four, exact radii, 1440/768/390/320px layouts, no overflow, dark/reduced modes, offscreen pause and no page errors. Screenshots: output/home-redesign/official-reference-svg-{desktop,mobile,dark}.png.
- Updated only temporary preview from /tmp/sub2api-home-official-svg-models-dist via cached-entry bridge. Public HTTP 200; no production lifecycle actions. Backup: /tmp/sub2api-home-pre-official-svg-models-backup.

### 2026-09-21 — Celestial artwork polish and round Astra
- Preserved section/layout/reveal sequence and Luna/Terra/Sol radii 26/48/80. Refined Luna into matte connected mare silhouettes, replaced crude Terra land polygons with public-domain Natural Earth orthographic vector coastlines and tapered cloud bands, and replaced scattered solar squiggles with eight restrained continuous ribbons.
- Astra now uses 480 stars without vertical compression. New regression test checks its horizontal/vertical extent ratio remains between 0.9 and 1.1.
- 33 tests passed; one known unrelated header choreography assertion excluded. Scoped ESLint, typecheck, isolated Vite build and whitespace checks passed. Chromium verified independent animation in all scenes, SVG-only rendering, size hierarchy, 1440/768/390/320 layouts, offscreen pause, reduced motion, dark mode and no page errors. Inspected desktop/mobile screenshots, including Earth coastline clipping.
- Screenshots: output/home-redesign/polished-svg-{desktop,mobile,dark}.png. Updated only authorized temporary preview from /tmp/sub2api-home-polished-svg-models-dist; cached HTML bridge resolves index-DTGwOKwd.js. Public HTTP 200. Backup: /tmp/sub2api-home-pre-polished-svg-models-backup. No production service changes.

### 2026-09-21 — Remove basketball-like Sol seams
- Removed all long solar ribbons, stroked rim and loop outlines. Sol now uses soft gradient-filled light cells, a breathing corona and small filled limb flames; no surface line network. Other scenes, sizes and section choreography unchanged.
- Updated regression assertion to reject stroked Sol artwork/old filaments. 13 component tests, scoped lint, typecheck and isolated build passed. Chromium checks passed for animation, all four viewport widths, reduced motion, offscreen pause and zero page errors. Desktop screenshot visually inspected; no crossing seams remain.
- Refreshed only temporary preview using /tmp/sub2api-home-soft-sol-dist (index-CdDavP7h.js). Backup: /tmp/sub2api-home-pre-soft-sol-backup. Screenshots: output/home-redesign/soft-sol-{desktop,mobile,dark}.png. Production untouched.

### 2026-09-21 — Stronger Sol / larger and slower Astra
- Removed solar polka-dot highlights. Replaced them with static monochrome-to-ember SVG turbulence, a hot limb, uneven filled corona and a restrained edge prominence. No raster assets or animated filter parameters; containing-layer motion remains slow and pausable. Desaturation before the warm color transfer prevents unwanted green noise patches.
- Astra outer radius increased from 84 to 112 viewBox units, with 720 stars. Revolution slowed from 45s to 240s, and core modulation slowed/softened to a 28s cycle. Luna/Terra and the size hierarchy/layout remain unchanged.
- 13 component tests passed, including larger round galaxy bounds and no old surface ellipses/stroked seams. Scoped lint, typecheck, isolated builds and diff check passed. Browser verified 240s computed rotation, native SVG plasma, independent motion, all four viewport widths, reduced motion, offscreen pause and zero page errors. Final desktop image inspected after correcting the texture palette.
- Only temporary preview refreshed, final build /tmp/sub2api-home-stellar-weight-final-dist, entry index-CoqyYqCu.js. Backups: /tmp/sub2api-home-pre-stellar-weight-backup and /tmp/sub2api-home-pre-stellar-weight-final-backup. Screenshots: output/home-redesign/stellar-weight-{desktop,mobile,dark}.png. No production lifecycle actions.

### 2026-09-21 — Brighter Sol and perceptible planetary motion
- Existing motions were running but too subtle: enlarged Luna's moving-shadow range and made its mare layer turn continuously (36s); increased Terra's land/cloud drift and tilt (14s/12s); changed Sol from a tiny 70s oscillation to continuous 60s surface motion, with 7s corona and 9s prominences. Astra remains enlarged and slow (240s).
- Lifted Sol's base/plasma transfer palette from dark red to luminous gold-orange, retaining texture. Enlarged the moving lunar shadow beyond its maximum excursion to prevent an uncovered bright strip at the limb; added regression coverage.
- 13 component tests, scoped lint, typecheck, isolated builds and whitespace checks passed. Browser checks covered actual running motion, responsive widths, 240s Astra rotation, reduced motion, offscreen pause and zero page errors. Additionally compared actual body screenshots separated by ~2.2s: pixels changing by >12/255 were Luna 57.4%, Terra 27.1%, Sol 57.4% (each above 10%), rather than merely asserting transform strings change.
- Final desktop visually inspected. Screenshots: output/home-redesign/visible-motion-{desktop,mobile,dark}.png and per-body before/after pairs. Only temporary preview refreshed from /tmp/sub2api-home-visible-motion-final-dist, entry index-_9xq30s4.js; public HTTP 200. Backups /tmp/sub2api-home-pre-visible-motion-backup and /tmp/sub2api-home-pre-visible-motion-final-backup. Production untouched.

### 2026-09-21 — Replace flat spins with spherical rotation
- Removed flat CSS surface rotation and lateral rocking from Luna/Terra/Sol, including the lunar moving shadow. Surface features now use longitude/latitude orthographic projection: limb foreshortening, rear-hemisphere clipping, and fixed projected axis/lighting. Luna/Terra/Sol periods are illustrative 96/64/80s; Astra stays at 240s.
- Added geographic Natural Earth coastlines (same public-domain source), a reproducible winding-normalization generator, and procedural lunar/plasma/cloud surface geometry. Promoted the already-installed d3-geo 3.1.1 and @types/d3-geo 3.1.0 to pinned direct dependencies; lock diff adds only these two importer entries, with no version upgrades. Removed the now-unused static preprojected coastline asset.
- SVG path updates use existing VueUse RAF lifecycle with a 24Hz cap, pause on offscreen/background/reduced-motion and teardown on unmount. No Canvas/WebGL/raster assets. Fine solar grain and illumination stay fixed while larger surface structures turn; retained bright gold-orange Sol.
- New mathematical tests verify limb compression without vertical shrink, backside hiding, full-turn return and projected bounds across all four quadrants. 18 scene/projection/showcase tests passed; lint/typecheck/isolated build/diff checks passed. Chromium verified changing projected paths with no CSS surface animations, actual offscreen/reduced-motion path freeze, 240s Astra rotation, all viewport widths, and no page errors. Visually inspected final desktop and sampled before/after planet frames.
- Only temporary preview refreshed from /tmp/sub2api-home-spherical-dist (index-BfkibcAk.js), backup /tmp/sub2api-home-pre-spherical-backup. Public HTTP 200. Screenshots: output/home-redesign/spherical-{desktop,mobile,dark}.png and per-body before/after frames. Production untouched.

### 2026-09-21 — Smaller Sol flame peak
- Changed only two animation peak scales: corona 1.055 → 1.015, prominences 1.07 → 1.015. Minimum sizes, opacity/brightness, spherical rotation and all other models unchanged.
- 14 tests, scoped lint, isolated build (including typecheck), diff check passed. Chromium paused both animations at their exact halfway peaks and confirmed scale 1.015 and opacity 1; Astra remains at 240s, no page errors. Peak screenshot visually inspected: output/home-redesign/sol-flame-cap-peak.png.
- Only temporary preview refreshed from /tmp/sub2api-home-sol-flame-cap-dist, entry index-C55k28dv.js. Backup /tmp/sub2api-home-pre-sol-flame-cap-backup. No production changes.

### 2026-09-21 — Natural broken Earth clouds
- Replaced 22 long latitude-aligned ellipses with 12 differently oriented, curved weather systems made from small irregular fragments with gaps. Added separate translucent veil/denser cloud layers at the same spherical drift speed. Oceans/continents remain visible; other models and Sol flame cap unchanged.
- Added projection regression checking both cloud layers at four longitudes: many separate fragments, each under 45 units wide on the 200-unit globe (no globe-spanning bands). 18 existing focused tests plus the new regression passed (19 current tests); lint, isolated build/typecheck and diff checks passed.
- Browser spherical-motion, reduced-motion/offscreen freeze, responsive widths and zero-page-error checks passed. Desktop and Terra detail visually inspected. Screenshots: output/home-redesign/natural-clouds-{desktop,mobile,dark}.png and natural-clouds-terra-detail.png.
- Updated only temporary preview from /tmp/sub2api-home-natural-clouds-dist, entry index-BhOs4e3d.js, backup /tmp/sub2api-home-pre-natural-clouds-backup. Production untouched.

### 2026-09-21 — Lunar surface relief
- Replaced equal-looking mare blobs with softer connected irregular regions, neutral mineral tones, subtle monochrome SVG grain and 111 varied geodesic craters (floors, dark inner rim crescents and lit rim fragments). Crater geometry shares the existing spherical projection; outline, size, rotation rate and fixed illumination are preserved. Other models unchanged.
- Added crater-layer rendering assertions and projection coverage across all four quadrants. 19 prior focused tests passed; updated scene/projection suite passed 16 tests (20 current total including the four showcase tests). Scoped lint, build/typecheck and diff checks passed. Browser confirmed SVG-only output, changing projected paths, offscreen/reduced-motion pause, responsive widths and no page errors.
- Desktop and lunar detail visually inspected. Screenshots: output/home-redesign/lunar-relief-{desktop,mobile,dark}.png and lunar-relief-detail.png. Procedural illustration, not a measured lunar relief map.
- Updated only temporary preview from /tmp/sub2api-home-lunar-relief-dist, entry index-D2tR_8RA.js; backup /tmp/sub2api-home-pre-lunar-relief-backup. Production untouched.

### 2026-09-21 — Softer illustrated Luna
- Removed lunar grain/displacement noise and reduced geodesic craters from 111 to 15. Retained a few shallow, low-contrast crater/rim accents; softened mare edges and shifted the base toward a quiet silver tone. Size, spherical rotation, lighting direction and other models unchanged.
- Updated regressions to reject lunar grain and cap visible crater density. 20 focused tests, lint, isolated build/typecheck and diff checks passed. Browser verified projected motion, SVG-only rendering, responsive layouts, offscreen/reduced-motion freeze and zero errors; desktop/detail visually inspected.
- Screenshots: output/home-redesign/lunar-soft-{desktop,mobile,dark}.png and lunar-soft-detail.png. Updated only temporary preview from /tmp/sub2api-home-lunar-soft-dist (index-DSmNNEXo.js), backup /tmp/sub2api-home-pre-lunar-soft-backup. Production untouched.

### 2026-09-21 — Large recognizable lunar craters
- Removed lunar mare shapes/filter entirely. Replaced small scattered craters with eight deliberately placed, larger landmark craters around the sphere (a few visible per face), with clearer floor shadows and lit rim crescents. No grain, gray mare patches or microdetail. Retained size, fixed lighting and spherical rotation; other models unchanged.
- Updated rendering/layer-density regressions. 20 tests, scoped lint, isolated build/typecheck and diff checks passed. Browser verified projection motion, SVG-only rendering, responsive widths, reduced-motion/offscreen freeze and no page errors. Desktop and moon detail visually inspected.
- Screenshots: output/home-redesign/lunar-landmarks-{desktop,mobile,dark}.png and lunar-landmarks-detail.png. Only temporary preview refreshed from /tmp/sub2api-home-lunar-landmarks-dist, entry index-C8KNjSC0.js; backup /tmp/sub2api-home-pre-lunar-landmarks-backup. Production untouched.

### 2026-09-21 — Horizontally center planetary bodies
- Set Luna/Terra/Sol body/surface/clip x-coordinate to 160 on the 320-unit viewBox, and aligned the solar corona transform origin. No changes to size, vertical placement, artwork or animation timing; Astra unchanged.
- Added center/clip assertions. 20 tests, scoped lint, isolated build/typecheck and diff checks passed. Chromium measured body/container horizontal centers at 1440/768/390/320px (all offsets <1px), and verified motion, pause/reduced-motion, SVG-only output and no page errors. Desktop visually inspected.
- Screenshots: output/home-redesign/centered-planets-{desktop,mobile,dark}.png. Only temporary preview refreshed from /tmp/sub2api-home-centered-planets-dist, entry index-Da9apuKs.js; backup /tmp/sub2api-home-pre-centered-planets-backup. Production untouched.

### 2026-09-21 — Restrained lunar surface finish
- Kept the eight large landmark craters and clean silhouette; added a low-contrast monochrome SVG finish at 12% opacity, with gentler pit shadows and lightly feathered rim edges. No additional craters, mare patches or dense gritty detail. Centering, size, spherical rotation and other models unchanged.
- Added texture-opacity regression (<=15%) while preserving the no-mare/no-old-grain assertions. 20 tests, lint, isolated build/typecheck and diff checks passed. Browser verified horizontal centers at four viewport widths, SVG-only motion, offscreen/reduced-motion freeze and zero errors. Desktop and lunar detail visually inspected.
- Screenshots: output/home-redesign/lunar-finish-{desktop,mobile,dark}.png and lunar-finish-detail.png. Only temporary preview refreshed from /tmp/sub2api-home-lunar-finish-dist (index-C4w2IZr3.js); backup /tmp/sub2api-home-pre-lunar-finish-backup. Production untouched.

### 2026-09-21 — Crater bowl lighting and tapered rims
- Render each of the eight lunar craters separately, allowing a local diagonal bowl gradient, soft basin bounce light and shaded inner wall per crater. Replaced blunt thick ring segments with tapered crescents and a thinner one-sided lit rim. Retained crater positions/outer radii, body size/centering, subtle texture, spherical rotation and all other model artwork.
- Added individual-crater clipping/full-turn projection coverage plus per-crater local-gradient rendering assertions. 21 focused tests, scoped lint, build/typecheck and diff checks passed. Chromium verified center positions at four viewport widths, changing SVG geometry, offscreen/reduced-motion freeze and zero page errors.
- Inspected normal-size Luna and a 4x-resolution capture to verify shaded depressions instead of flat discs. Screenshots: output/home-redesign/crater-lighting-{desktop,mobile,dark}.png and crater-lighting-detail-4x.png.
- Only temporary preview refreshed from /tmp/sub2api-home-crater-lighting-dist, entry index-CaCMxPTv.js; backup /tmp/sub2api-home-pre-crater-lighting-backup. Public HTTP 200; production untouched.

### 2026-09-21 — Crater silhouette and interior structure
- Replaced perfect geodesic circles with lightly irregular outlines, varying/tapered rims, flatter inset floors and a restrained inner terrace. Added small two-facet central relief only to the three larger craters. Removed glossy basin-light treatment; retained local wall shading and subtle surface finish. Nominal crater positions/radii, body centering, size, rotation and other models unchanged.
- Extended regressions for terrace/floor distinction, large-only central peaks, hidden faces and finite in-sphere projected coordinates at four rotation angles. 21 focused tests passed before assertion extensions; the updated 17-test projection/scene suite passed afterward. Scoped lint, isolated build/typecheck and diff checks passed.
- Browser verified horizontal centering at four widths, changing projected SVG paths, offscreen/reduced-motion freeze and zero page errors. Normal-size lunar frame and 4x-detail visually inspected. Screenshots: output/home-redesign/crater-character-{desktop,mobile,dark}.png and crater-character-detail-4x.png.
- Only temporary preview refreshed from /tmp/sub2api-home-crater-character-dist, entry index-Cct2QKxg.js; backup /tmp/sub2api-home-pre-crater-character-backup. Production untouched.

## Sticky header and focused section slider — 2026-09-21

### Scope
- Implemented a native, page-local six-chapter vertical slider with direct chapter controls (right rail on desktop; bottom dock on phone/tablet), smooth scrolling, active state, and restrained content transitions.
- Converted the homepage header to sticky positioning with an animated, inset compact surface. A fixed-height track and non-collapsing header formatting context prevent scroll-target/layout shifts when contracting.
- Moved the existing supported-models section directly after the everyday-features section. No brand or celestial artwork/animation changes; no new dependencies or configuration.
- Preserved long section readability, native touch/wheel scrolling, local arrow/Home/End chapter navigation, guide/skip-link focus, deep-linked hashes, custom homepages, authenticated destinations, and reduced-motion behavior. Clipped per-slide entry transforms to avoid previous-section fragments showing under the sticky header.

### Verification
- All **69 targeted homepage tests** passed (8 files), including 7 new slider tests and updated homepage ordering/section-control coverage: `/tmp/sub2api-slider-test.log`.
- Full frontend lint: **0 errors**, the same 12 unrelated warnings. Typecheck passed.
- Full frontend suite: **969 passed / 5 failed**, 141 passing files and the same four baseline failing files already recorded above: `usePersistedPageSize`, `EmailOAuthButtons`, `HelpTooltip`, two date assertions in `SubscriptionsView`. No unrelated fixes made. Logs: `/tmp/sub2api-slider-full-{lint,test}.log`, `/tmp/sub2api-slider-typecheck.log`.
- Isolated build passed: `/tmp/sub2api-home-section-slider-verified-dist`, log `/tmp/sub2api-home-section-slider-verified-build.log`. No default frontend/embedded backend dist build.
- Comprehensive browser checks passed for all six direct chapter jumps, native wheel scrolling, sticky positioning, viewport alignment, return-to-top, keyboard Home/End, guide-link focus, 1440/768/390/320px layouts without horizontal overflow, mobile long-content scroll, dark mode, reduced motion, and incoming model-section hash. No page errors: `/tmp/sub2api-slider-browser.log`.
- Existing celestial browser regression passed: SVG-only render, 26/48/80 radii, centered bodies, all four animations, slow Astra, offscreen freeze, reduced-motion freeze, and responsive layouts. Log: `/tmp/sub2api-slider-planets-browser.log`.

### Preview-only publication
- Refreshed the existing authorized isolated static preview on `http://152.53.90.186:4178/home`; public HTTP 200.
- Publisher `/tmp/sub2api-publish-section-slider-verified-preview.py`; new entry `/assets/index-FHR5fY3M.js`; previous cached entry/CSS/index backup `/tmp/sub2api-home-pre-section-slider-verified-backup`.
- No production process/container/proxy/database/image changes. No commit/push.
- Trellis workflow/start instructions and relevant guidelines read. The referenced `.trellis/scripts/get_context.py` is absent from this checkout; context was inspected directly.
- Final-build follow-up browser check also passed: stable 12px header inset, clipped slide boundaries, freely scrolling over-height desktop policy slide, actual CDP touch swipe and touch chapter selection on 390px mobile, reachable billing details, and zero runtime errors. Log `/tmp/sub2api-slider-final-browser.log`. Final light/dark/desktop/mobile screenshots copied to `output/home-redesign/sub2api-slider-final-*.png` and visually inspected.

## Stable glass header and staged chapter motion — 2026-09-21

- Replaced simultaneous header width/height/logo-scale changes with a backdrop-only pseudo-element transition. Foreground layout and fixed header track never change; floating state has a 64%-opaque page tint, 24px backdrop blur, rounded inset edges and restrained shadow. Removed the opaque parent background so this is actual backdrop glass. Entry/exit hysteresis (64px / 8px) avoids state chatter.
- Removed browser scroll snapping, which could pull short scrolls back and reverse the header state. Native wheel/touch movement is uninterrupted. Explicit chapter/guide navigation now dissolves outgoing content for 220ms, repositions while faded, and restores the incoming scene instead of rapidly passing through intermediate chapters. New user scroll/touch/pointer/keyboard input cancels pending navigation; subsequent chapter requests replace earlier ones. Reduced-motion/hash navigation is immediate.
- Marked individual eyebrows, headings, copy, buttons, feature cards, steps, policies, footer and model-gallery beats for staggered 18px arrivals. Reset only after a whole chapter exits, preserving reading stability in long mobile sections. Added independent outer model-card reveal wrappers without changing SVG artwork, physical motion or the initial brand sequence.
- **74 targeted homepage tests passed**, including new navigation interruption/replacement, threshold stability, viewport reveal/reset, and stagger ordering coverage. Full lint: zero errors / same 12 unrelated warnings; typecheck and isolated Vite build passed. Logs: `/tmp/sub2api-fluid-test.log`, `/tmp/sub2api-fluid-lint.log`, `/tmp/sub2api-fluid-typecheck.log`.
- Browser samples verified the logo/action x/y/width/height remain unchanged (tolerance <0.15px) throughout the header transition. Verified actual backdrop styles, outgoing dissolve before the scroll-position change, title-before-cards and first/second/third card stagger, replay on reentry, 1440/768/390/320px layouts, reachable mobile billing content, dark mode, visible nonanimated reduced-motion content, and zero runtime errors. Log: `/tmp/sub2api-fluid-browser.log`.
- Existing celestial browser regression passed, including SVG-only rendering, relative radii, centered planets, four animations, offscreen pause and reduced-motion freeze: `/tmp/sub2api-fluid-planets.log`. Screenshots visually inspected and copied to `output/home-redesign/sub2api-fluid-*.png`.
- Preview-only build: `/tmp/sub2api-home-fluid-slides-verified-dist`; entry `/assets/index-GaGop4IR.js`; publisher `/tmp/sub2api-publish-fluid-slides-verified-preview.py`; backup `/tmp/sub2api-home-pre-fluid-slides-verified-backup`. Public preview HTTP 200. No production changes, commit or push.
- Full frontend suite: **974 passed / the same 5 baseline failures** (141 passing / 4 failing files). Unchanged failures remain page-size default, affiliate OAuth parameter, tooltip hover, and the two subscription date assertions. Log `/tmp/sub2api-fluid-full-tests.log`.

## Header settling stability and clearer elevation — 2026-09-21

- User observed a final settling shake despite stable foreground positions. Removed geometric animation of the blur surface entirely: expanded and floating pseudo-element planes now retain fixed inset/radius/filter/shadow geometry and crossfade only opacity over 500ms. Persistent `will-change: opacity` remains after the transition, avoiding an end-of-transition compositing hint change. Header/brand/action layout and scroll thresholds are unchanged.
- Strengthened floating elevation with separate near-contact and broad ambient shadows, plus a dark-mode shadow variant. Shadow dimensions/color remain fixed throughout the crossfade rather than finishing on a different timing curve.
- 34 homepage/slider tests passed, scoped ESLint passed, isolated Vite build/type checking passed, and `git diff --check` passed. No functional JavaScript, model, slider, or production changes.
- Browser frame samples at 1440/768/390/320px verified that both planes' inset, radius, shadow and blur stay exactly fixed, brand/action rectangles stay identical, opacity progresses monotonically through the final frames, and compositing hints remain present. Reverse transitions, reduced motion, stronger light/dark shadows and zero runtime errors verified. Log `/tmp/sub2api-stable-glass-browser.log`; screenshot copies `output/home-redesign/sub2api-stable-glass-shadow-*.png` visually inspected.
- Authorized static preview refreshed: public HTTP 200. Build `/tmp/sub2api-home-stable-glass-dist`; entry `/assets/index-C4JWB9fN.js`; publisher `/tmp/sub2api-publish-stable-glass-preview.py`; previous cached assets backup `/tmp/sub2api-home-pre-stable-glass-backup`. Production untouched; no commit/push.

## Aether-referenced continuous chapter transitions — 2026-09-21

### Reference and scope
- Inspected the local Aether homepage and section-animation helper read-only (`/opt/stacks/aether/frontend/src/views/public/{Home.vue,useSectionAnimations.ts}`). Its relevant pattern is continuous native smooth/snap scrolling with viewport-driven staged content, not a whole-page dissolve. Implemented that interaction independently; no Aether source/assets copied, no Aether files or services modified.
- Removed the outgoing-main fade, delayed instant reposition and navigation timer. Chapter/guide controls now move continuously through measured native smooth scrolling. Desktop chapters snap; phone/tablet use proximity snapping and over-height sections remain freely readable. Rapid pointer requests redirect native smooth scrolling without an intermediate stop/snap.
- Fit-to-viewport content follows continuous viewport coverage through staggered thresholds for headings/copy/cards; guide columns have opposite-side entry offsets. A short initial ramp stages the first chapter. Long sections retain already-revealed content until the whole section leaves, protecting mobile policy readability. Reverse scrolling naturally reverses progress instead of triggering a blank-screen swap.
- Preserved the current two-plane glass header, stronger shadows, model order, SVG artwork/physical motion, custom homepages and public/auth destinations.

### Verification and preview
- 75 targeted homepage tests passed (8 files), plus the final 12-test slider retarget regression. Scoped/full lint passed with the same 12 unrelated warnings, standalone typecheck passed, and isolated build passed. The first combined quality-run shell was terminated during typecheck (143); rerun completed normally. Full suite: 975 passed, the same five unrelated baseline failures in four files.
- Browser sampled >10 intermediate scroll positions for one chapter change; `main` opacity stayed 1 throughout, progress was monotonic and feature-card thresholds were ordered. Checked forward/back scrolling, native wheel alignment, header/shadows, keyboard/guide/hash navigation, long mobile content, 1440/768/390/320px layouts, dark/reduced-motion and no runtime errors. Log `/tmp/sub2api-continuous-slides-final-browser.log`.
- Celestial regression passed (SVG-only, sizes/centering, actual motion, offscreen/reduced-motion freeze): `/tmp/sub2api-continuous-slides-planets.log`. Final follow-up verifies real rapid pointer retargeting without backwards snap and scrolling through an over-height policy chapter on a short desktop viewport: `/tmp/sub2api-continuous-slides-retarget-browser.log`.
- Tests/quality logs: `/tmp/sub2api-continuous-slides-final-tests.log`, `/tmp/sub2api-continuous-slides-pointer-tests.log`, `/tmp/sub2api-continuous-slides-lint.log`, `/tmp/sub2api-continuous-slides-typecheck-final.log`, `/tmp/sub2api-continuous-slides-full-tests-final.log`. Screenshots including a mid-transition frame copied to `output/home-redesign/sub2api-continuous-*.png` and visually inspected.
- Preview-only build `/tmp/sub2api-home-continuous-slides-verified-dist`; entry `/assets/index-C3MGQYPl.js`; publisher `/tmp/sub2api-publish-continuous-slides-verified-preview.py`; backup `/tmp/sub2api-home-pre-continuous-slides-verified-backup`. Public preview HTTP 200. No production lifecycle, database, image, proxy, commit or push changes.

## Everyday SVG feature carousel — 2026-09-21

### Scope
- Preserved all existing everyday feature cards/copy; appended a responsive editorial carousel within the same section, still before supported models. Original SVG scenes illustrate intelligence (woven core/converging signals), speed (three routes/response waveform) and trust (open ledger/verification seal). No remote artwork, new dependencies, account requests or fabricated usage data; the approximate WS latency is repeated from the existing user-specified copy.
- Added Chinese/English copy, automatic eight-second rotation, soft crossfades, direct selection and arrow/Home/End support. A CSS progress animation owns the rotation clock; no JS idle timer or render loop. Viewport/document visibility gate motion; hover/keyboard focus pause automatic navigation, while decorative motion continues for manual viewing. Reduced motion is static and manually navigable. Original header, chapter controller, model scenes and custom-homepage branches are unchanged.
- Reduced only the everyday section's excessive spacing and capped art height so the appended scene fits together with original cards at 1440×900 (788px section / 796px available). Longer mobile/short-desktop sections retain native scroll.

### Checks
- All 86 homepage tests (9 files) passed, including 10 new carousel tests and one integration test. Standalone typecheck, scoped lint and isolated Vite build passed; full lint retained only 12 unrelated baseline warnings. Full frontend run: 986 passed, same five baseline failures across four files (persisted page size, email OAuth affiliate param, tooltip hover, two subscription date/timezone assertions). No failures attributable to this change.
- Playwright verified real eight-second autoplay and wraparound, moving SVG strokes, pause/resume, manual selection, keyboard navigation without chapter switching, offscreen suspension, reduced motion, dark mode, 1440/1280/768/390/320px overflow containment, localized English and no runtime errors. Header shadow/state preserved. Script `/tmp/sub2api-home-browser/feature-carousel-check.cjs`, log `/tmp/sub2api-feature-carousel-final-browser.log`.
- Logs: `/tmp/sub2api-feature-carousel-final-home-tests.log`, `/tmp/sub2api-feature-carousel-final-typecheck.log`, `/tmp/sub2api-feature-carousel-lint.log`, `/tmp/sub2api-feature-carousel-full-tests.log`, `/tmp/sub2api-home-feature-carousel-final-build.log`. Final desktop, speed/trust, dark and phone screenshots visually inspected and copied into `output/home-redesign/sub2api-feature-carousel-final-*.png`.
- Final preview-only build: `/tmp/sub2api-home-feature-carousel-final-dist`; entry `/assets/index-D4LPOo1S.js`; publisher `/tmp/sub2api-publish-feature-carousel-final-preview.py`; immediate pre-final backup `/tmp/sub2api-home-pre-feature-carousel-final-backup`, pre-feature backup `/tmp/sub2api-home-pre-feature-carousel-backup`. Existing static server refreshed without restart; public preview HTTP 200. No production/container/proxy/database changes, no commit/push.
- Final existing chapter-motion and celestial browser regressions also passed unchanged: `/tmp/sub2api-feature-carousel-chapters.log` (continuous/reverse scrolling, staged entry, native wheel snap, anchors, mobile long content, reduced/hash modes) and `/tmp/sub2api-feature-carousel-planets.log` (SVG-only, common-scale radii/centering, four distinct animations and visibility pause).

## Unified feature controls and pricing close — 2026-09-21

### Scope
- Moved the original three feature cards into the carousel as its sole controls. Preserved icon/title/body copy, added full-card native button hit targets, active styling, controlled/described-by links and synchronized progress. Cards and art share one surface; removed the redundant lower selector strip, retaining only the discreet pause/resume button in the art corner. Original-card keyboard selection and automatic SVG rotation share the same state. Artwork visibility, rather than combined-card height, gates animation on phones.
- Removed the separate closing chapter. All its content/action now appears directly after pricing cards inside the pricing section; site footer follows in the same chapter. Navigation has five stops and pricing is the final/back-to-top chapter. Preserved public/auth destinations, configured footer links, section-entry motion, existing model artwork and glass header.

### Verification
- 87 homepage tests passed (9 files), including original-card controls, no duplicate selector set, synced active state and closing placement/action assertions. Full lint: no errors, same 12 baseline warnings. Standalone typecheck and isolated build passed. Full frontend: 987 passed / same five unrelated baseline failures in four files.
- Playwright verified actual eight-second autoplay and matching card highlight, clicks on original card bodies (not just heading text), manual/keyboard scene selection without changing chapters, five chapter controls, pricing/CTA/footer order, retained login destination, last-chapter return-to-top, mobile long-content scroll, 1440/768/390/320px overflow containment, dark/reduced motion and no runtime errors. Desktop everyday section measured 735px and fits the available 796px. Screenshots visually inspected and copied to `output/home-redesign/sub2api-unified-features-*.png`.
- Logs: `/tmp/sub2api-unified-features-home-tests.log`, `/tmp/sub2api-unified-features-full-tests.log`, `/tmp/sub2api-unified-features-lint.log`, `/tmp/sub2api-unified-features-typecheck.log`, `/tmp/sub2api-home-unified-features-build.log`, `/tmp/sub2api-unified-features-browser.log`. Browser script `/tmp/sub2api-home-browser/unified-features-check.cjs`.
- Preview-only build `/tmp/sub2api-home-unified-features-dist`, entry `/assets/index-CaRPspIC.js`, publisher `/tmp/sub2api-publish-unified-features-preview.py`, previous-preview backup `/tmp/sub2api-home-pre-unified-features-backup`. Existing static preview assets refreshed without restarting services. Production, containers, proxies and databases unchanged; no commit/push.
- Final chapter-motion regression passed using the existing continuous-scroll script adapted only from a sixth-stop selector to the last/fifth stop: `/tmp/sub2api-home-browser/unified-chapters-check.cjs`, log `/tmp/sub2api-unified-features-chapters.log`. Verified staged card entry, continuous bidirectional scrolling, native wheel snap, preserved header/shadow, guide/keyboard/hash navigation and long mobile content.

## Roomier non-hero chapters and redrawn SVG scenes — 2026-09-21

### Scope and layout
- Followed the expanded request: slide 1 unchanged; all later slides get additional vertical capacity and increased section/internal spacing. Non-hero minimum height is available viewport plus two section gutters, not a maximum-height one-screen fit. Shared section spacing, larger model brand/card separation, roomier guide steps and pricing/callout gaps preserve content hierarchy and native scrolling.
- Removed every redundant scene caption/headline/description/footnote and its unused locale entries. The original feature cards remain the only text/selection controls. Independently redrew all three SVGs as multi-part full-width scenes (neural processing, multi-route transport, provenance/receipt/audit), with distinct animated details, aligned connections and no invented account figures.
- Fixed-aspect 1200×520 landscape artwork recomposes all main/supporting elements into 600×720 portrait below 640px. No raster assets, cropping, aspect distortion, new libraries, remote data calls or JS animation loops.

### Verification
- 94 homepage tests passed (10 files), including seven new SVG composition/proportion/details tests and revised no-caption assertions. Lint/typecheck/isolated build passed; full lint has the same 12 unrelated warnings. Full frontend: 994 passed, same five unrelated failures (persisted page size, OAuth affiliate parameter, hover tooltip, two subscription timezone assertions).
- Playwright compared before/after geometry at 1440×900: hero and header rectangles exactly unchanged; later chapter heights 796→1144, 796→976, 796→976 and 969→1108px. Illustration stage 315→617px. All later sections also increased internal spacing. Recorded baseline `/tmp/sub2api-roomy-scenes-before.json`.
- Browser checks verified real autoplay/synchronized feature selection, animated SVG strokes, no redundant scene text, complete proportional landscape/portrait detail containment at 1440/1280/768/390/320px, native scrolling within enlarged chapters, retained closing/footer access, offscreen suspension, dark/reduced-motion behavior, and no runtime errors. Main script `/tmp/sub2api-home-browser/roomy-scenes-check.cjs`; log `/tmp/sub2api-roomy-scenes-final-browser.log`. Final desktop/phone art, model, guide and pricing screenshots visually inspected and copied to `output/home-redesign/sub2api-roomy-scenes-final-*.png`.
- Quality logs: `/tmp/sub2api-roomy-scenes-final-home-tests.log`, `/tmp/sub2api-roomy-scenes-full-tests.log`, `/tmp/sub2api-roomy-scenes-lint.log`, `/tmp/sub2api-roomy-scenes-final-typecheck.log`, `/tmp/sub2api-home-roomy-scenes-final-build.log`. An initial test erroneously matched a retained original-card phrase; it was scoped to the scene panel, and the full homepage rerun passed.
- Final preview build `/tmp/sub2api-home-roomy-scenes-final-dist`; entry `/assets/index-DYDAi7Xg.js`; publisher `/tmp/sub2api-publish-roomy-scenes-final-preview.py`; immediate prior assets `/tmp/sub2api-home-pre-roomy-scenes-final-backup`, pre-change preview `/tmp/sub2api-home-pre-roomy-scenes-backup`. Refreshed existing isolated static preview without a service restart. No production/container/proxy/database changes, commit or push.
- Existing celestial browser regression also passed after the spacing changes: `/tmp/sub2api-roomy-scenes-planets.log` (SVG-only, 26/48/80 radii, centered bodies, all four animations, unchanged Astra duration, offscreen freeze, dark/reduced modes and responsive containment).

## Correct desktop chapters back to one screen — 2026-09-22

### Scope
- Removed the erroneous later-chapter minimum of viewport plus twice the section gutter. Kept the native one-viewport minimum and original first-screen/header composition.
- For desktop ≥1024×700, added height-aware gutters and intrinsic-text/flexible-art grid layouts. Feature/model SVGs consume remaining space while preserving aspect ratio; minimum-content sizing and explicit art minimums retain a natural long-content fallback. Model caption space is separate, and the brand's static/animated settled scale agrees.
- Compacted desktop pricing padding/gaps and typography sufficiently to target a complete policy + closing + footer chapter without hiding copy. Mobile/short layouts remain scrollable. No production changes, dependency additions, rewritten SVG artwork or page-wide scaling.

### Checks and limitations
- 94 homepage tests passed; standalone typecheck, lint and isolated Vite build passed. Lint retains 12 unrelated warnings. Full frontend: 994 passing and the same five unrelated baseline failures across four files.
- Network access and browser launch are restricted in this session. The localhost probe returned HTTP 000; normal Chromium launch failed with `sandbox_host_linux.cc` / `shutdown: Operation not permitted`. No permission or network workaround attempted. **No live screenshot, one-screen geometry or HTTP health verification is claimed.** Observations recorded at `/tmp/sub2api-screen-fit-browser-limit.txt`.
- Prepared `/tmp/sub2api-home-browser/screen-fit-check.cjs`: assertions for all five chapters, every later chapter fitting below the header at 1366×768 / 1440×900 / 1920×1080, Chinese/English copy, visible reveal targets, preserved pricing callout/footer and readable mobile/short-screen fallback. Syntax check passed; browser execution remains pending.
- Quality logs: `/tmp/sub2api-screen-fit-home-tests.log`, `/tmp/sub2api-screen-fit-full-tests.log`, `/tmp/sub2api-screen-fit-lint.log`, `/tmp/sub2api-screen-fit-typecheck.log`, `/tmp/sub2api-home-screen-fit-build.log`.
- Preview **files** updated offline from `/tmp/sub2api-home-screen-fit-dist`, entry `/assets/index-AVPLmMPK.js`, using `/tmp/sub2api-publish-screen-fit-preview.py`. Recorded cached alias `index-CXxW4uoe.js` and prior current disk entry `index-DYDAi7Xg.js` now bridge to the new build; their CSS aliases were refreshed as well. Backed up all affected files to `/tmp/sub2api-home-pre-screen-fit-backup`; relative imports validated locally. No service restart, production/container/proxy/database change, commit or push.


## Use the final chapter's vertical space — 2026-09-22

- User clarified **vertical**, not horizontal, space. Changed only desktop pricing CSS in `HomeView.vue`: its content wrapper fills the available height; the policy grid expands between the heading/notice and closing/footer, with vertically centered card contents and bounded height-responsive block padding. Kept the existing width, copy, typography, first four slides, header, motion and narrow/short fallback. No forced viewport-plus-gutters sizing, cropping or page scaling.
- 94 homepage tests passed (10 files). Lint and typecheck passed; the same 12 unrelated lint warnings remain. Full frontend: 994 passed / five existing failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription date assertions). Isolated Vite build and `git diff --check` passed.
- Logs: `/tmp/sub2api-pricing-vertical-home-tests.log`, `/tmp/sub2api-pricing-vertical-lint.log`, `/tmp/sub2api-pricing-vertical-typecheck.log`, `/tmp/sub2api-pricing-vertical-full-tests.log`, `/tmp/sub2api-home-pricing-vertical-build.log`.
- Prepared and syntax-checked `/tmp/sub2api-home-browser/pricing-vertical-check.cjs`, extending the existing single-screen checks with full-height final-content coverage, bottom-aligned footer, stretched equal-height cards, no overlap and unchanged width. **Not browser-executed:** the session retains the previously recorded Chromium/network sandbox restriction; no screenshot, live HTTP health or geometry verification is claimed, and no workaround was attempted.
- Refreshed only authorized preview files offline from `/tmp/sub2api-home-pricing-vertical-dist`, entry `/assets/index-JKua_HTX.js`, using `/tmp/sub2api-publish-pricing-vertical-preview.py`. Cached `index-CXxW4uoe.js` and prior `index-AVPLmMPK.js` entries bridge to the new build; CSS aliases refreshed, imports checked, changed files backed up at `/tmp/sub2api-home-pre-pricing-vertical-backup`. No service restart, production/container/proxy/database changes, commit or push.


## Only expand the gap before Make room for an idea — 2026-09-22

- Corrected the previous interpretation: restored original card padding and block layout; removed flexible policy-row sizing. The first three pricing rows use intrinsic heights, while the final row absorbs spare space with its closing callout bottom-aligned. Only the gap between Usage & Pricing and Make room for an idea expands. Original minimum spacing, width, copy, other slides and narrow/short fallback remain unchanged.
- 94 homepage tests passed; scoped HomeView ESLint and isolated Vite build (including the configured Vue type checker) passed; `git diff --check` passed. Logs: `/tmp/sub2api-pricing-gap-home-tests.log`, `/tmp/sub2api-pricing-gap-lint.log`, `/tmp/sub2api-home-pricing-gap-build.log`. Full frontend suite was not rerun for this CSS-only correction; the immediately preceding run remains recorded above.
- Prepared/syntax-checked `/tmp/sub2api-home-browser/pricing-gap-check.cjs`, replacing the prior flexible-card expectation with a comparison against intrinsic card height while retaining full-height/footer/no-overlap assertions. Browser/HTTP validation remains unavailable under the previously recorded restriction; no geometry or screenshot result claimed.
- Updated authorized preview files offline from `/tmp/sub2api-home-pricing-gap-dist`, entry `/assets/index-9UI8YW68.js`, publisher `/tmp/sub2api-publish-pricing-gap-preview.py`, backup `/tmp/sub2api-home-pre-pricing-gap-backup`. Refreshed cached/current JS and CSS aliases; imports validated. No service restart, production change, commit or push.


## Separate billing; simplify setup and add request-content retention — 2026-09-22

- Replaced chapter four's full setup guide with `HomeBillingSection.vue`: presale-only monthly subscriptions, usual start/end-of-month period and lower pricing; immediate balance top-ups, actual-use charges and the Astra 0.4–0.5 multiplier. Two intrinsic-height cards with six labeled details, no invented package amounts or private statistics. Existing locale/theme/public-auth routing retained.
- Moved Your first sip to the beginning of Usage & Pricing as a compact three-step guide, preserving the configured documentation link. Replaced the old billing card with request controls. Both languages now explicitly state audit-only use, **one-day ordinary request-content retention**, and **permanent retention for Cyber requests and policy-violating request content**. No seven-day copy. This is homepage presentation only; backend auditing, deletion schedules and production data are untouched. Asked asynchronously for the exact request-format restriction; pending a specific rule, text only directs visitors to use Codex/Pi according to the setup documentation, without inventing a protocol or banning custom formatting.
- Five chapters remain. Added nested-anchor resolution so existing `#getting-started` clicks and incoming hashes resolve to the enclosing final chapter with focus/modifier/reduced-motion behavior preserved. Intrinsic policy rows and the flexible gap before the closing callout remain; narrow/short screens retain scrolling. Brand/feature/model art and animations unchanged.
- 99 homepage tests passed (10 files), including new ordering, simplified guide, detailed billing, both-language retention and nested-anchor/hash assertions. Full frontend: 999 passed and the same five unrelated baseline failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription date assertions). Full lint/typecheck/isolated build and diff check passed; lint retains the same 12 unrelated warnings.
- Logs: `/tmp/sub2api-home-billing-policy-tests.log`, `/tmp/sub2api-home-billing-policy-full-tests.log`, `/tmp/sub2api-home-billing-policy-lint.log`, `/tmp/sub2api-home-billing-policy-typecheck.log`, `/tmp/sub2api-home-billing-policy-build.log`. Prepared/syntax-checked `/tmp/sub2api-home-browser/billing-policy-check.cjs` for viewport geometry, intrinsic cards, nested guide navigation, policy copy, and short/mobile fallback. Browser/HTTP validation is still unavailable under the recorded sandbox restriction; no screenshot, one-screen fit or live HTTP result is claimed.
- Refreshed only the authorized preview files offline from `/tmp/sub2api-home-billing-policy-dist`, entry `/assets/index-BabQLSwH.js`, using `/tmp/sub2api-publish-billing-policy-preview.py`. Cached and prior disk entries/CSS aliases refreshed, local imports validated, affected files backed up at `/tmp/sub2api-home-pre-billing-policy-backup`. No restart, production/container/proxy/database change, commit or push.


## Refine request-control copy and hierarchy — 2026-09-22

- Replaced three dense label/paragraph rows with a concise format/audit-only summary and a two-column semantic definition list: ordinary request content → one day only; Cyber and rule-violating request content → permanent retention. Shortened both locales without changing either scope or duration. Thin top/center dividers and restrained larger duration text; columns stack below 480px. No extra disclosure interaction, card stretching, backend retention change or unrelated chapter edits.
- Updated both-locale tests to assert the correct content category is paired with its duration. All 99 homepage tests passed; full lint/typecheck and isolated Vite build passed (same 12 unrelated lint warnings). `git diff --check` passed. Full frontend suite was not rerun for this copy/layout-only refinement; the preceding baseline is recorded above.
- Logs: `/tmp/sub2api-request-display-tests.log`, `/tmp/sub2api-request-display-lint.log`, `/tmp/sub2api-request-display-typecheck.log`, `/tmp/sub2api-home-request-display-build.log`. Prepared/syntax-checked `/tmp/sub2api-home-browser/request-display-check.cjs` with updated retention selectors/copy. Browser/HTTP validation remains unavailable under the previously recorded sandbox restriction; no live visual/layout verification claimed.
- Updated only authorized preview files from `/tmp/sub2api-home-request-display-dist`, entry `/assets/index-CzSG6R65.js`, publisher `/tmp/sub2api-publish-request-display-preview.py`, backup `/tmp/sub2api-home-pre-request-display-backup`. Cached/current JS and CSS aliases refreshed, relative imports checked. No restart, production changes, commit or push.


## Rebalance the quick guide and separate it from Usage Policy — 2026-09-22

- Replaced the crowded single row with a title/docs header and a separate three-equal-column step row; added restrained dividers and a 24–32px bottom margin before the usage notice. The title/eyebrow remain compact, configured documentation stays optional, and phone steps stack. Kept all copy, links, auth routing, progressive reveals, intrinsic policy cards and flexible closing gap unchanged.
- Updated HomeView assertions for header-before-steps structure and documentation placement. All 99 homepage tests passed; scoped ESLint and isolated Vite build (including configured Vue type checker) passed; diff check passed. Full frontend suite not rerun for this markup/CSS-only adjustment. Logs: `/tmp/sub2api-quick-guide-layout-tests.log`, `/tmp/sub2api-quick-guide-layout-lint.log`, `/tmp/sub2api-home-quick-guide-layout-build.log`.
- Prepared/syntax-checked `/tmp/sub2api-home-browser/quick-guide-layout-check.cjs`, adding equal-column/top alignment, header-before-steps and minimum usage-notice gap checks to the existing desktop/mobile regression. Browser/HTTP execution remains unavailable under the recorded sandbox restriction; no actual screenshot or geometry verification claimed.
- Preview files updated offline from `/tmp/sub2api-home-quick-guide-layout-dist`, entry `/assets/index-BCIyYkNk.js`, publisher `/tmp/sub2api-publish-quick-guide-layout-preview.py`, backup `/tmp/sub2api-home-pre-quick-guide-layout-backup`. Cached/current entries and CSS aliases refreshed; relative imports validated. No restart, production changes, commit or push.


## Remove external-link glyphs from client buttons — 2026-09-22

- Removed only the small external-link arrow glyphs from the Codex and Pi client buttons. Brand marks, labels, official URLs, new-tab safety attributes, layout and behavior are unchanged.
- HomeView tests: 29 passed; scoped ESLint and isolated Vite build passed; diff check passed. Preview files refreshed offline from `/tmp/sub2api-home-client-links-dist`, with backup `/tmp/sub2api-home-pre-client-links-icon-backup`. No restart or production changes; browser/HTTP verification remains unavailable in this sandbox.


## Match homepage primary actions to Sub2API — 2026-09-22

- Applied the existing global `btn btn-primary` classes to both homepage entry actions and removed the conflicting flat page-local fill/brightness hover. Homepage dimensions are preserved, while color tokens, gradient, shadow, pointer-aware sheen and active state now come from the same shared style as the rest of Sub2API. Updated radius to the shared 12px treatment.
- HomeView tests: 30 passed, including a new shared-class assertion; scoped ESLint and isolated Vite build passed; diff check passed. Preview files refreshed offline from `/tmp/sub2api-home-primary-button-dist`, with backup `/tmp/sub2api-home-pre-primary-button-backup`. No production/restart changes; browser/HTTP visual verification remains unavailable.


## Faster ChatGPT intro and persistent manual brand stage — 2026-09-22

- Shortened the one-time brand/model reveal from approximately 4.7s to 3.6s by tightening draw/fill/echo/settle timing and model-card staggering. Added a 36s continuous ChatGPT-mark rotation isolated from existing draw/layout transforms; it runs in both stages, pauses with the existing visibility gate and is disabled for reduced motion.
- Converted only the ChatGPT mark to an accessible button. It is disabled during the initial sequence. Once models are shown, click/keyboard activation expands the brand and hides/pauses model scenes. Manual brand selection persists indefinitely—there is no timer or automatic return—and a second activation manually restores the models. The initial visit alone auto-advances. Added bilingual stage-specific labels and `aria-pressed` state.
- 101 homepage tests passed (10 files), including faster delay values, initial stage completion, persistent manual brand selection, explicit manual return, rotation wrapper, reduced motion and homepage integration. Scoped ESLint, standalone typecheck, isolated Vite build and diff check passed. Logs: `/tmp/sub2api-model-brand-toggle-tests.log`, `/tmp/sub2api-model-brand-toggle-home-tests.log`, `/tmp/sub2api-model-brand-toggle-typecheck.log`, `/tmp/sub2api-home-model-brand-toggle-build.log`.
- Prepared/syntax-checked `/tmp/sub2api-home-browser/model-brand-toggle-check.cjs` for automatic completion, 36s running rotation, persistent manual brand after a four-second hold, explicit return, scene visibility and reduced motion. Browser/HTTP execution remains unavailable under the recorded sandbox restriction; no live timing/visual result is claimed.
- Preview files refreshed offline from `/tmp/sub2api-home-model-brand-toggle-dist`, entry `/assets/index-BeVrvkFV.js`, publisher `/tmp/sub2api-publish-model-brand-toggle-preview.py`, backup `/tmp/sub2api-home-pre-model-brand-toggle-backup`. Cached/current entry and CSS aliases refreshed; relative imports validated. No restart, production changes, commit or push.


## Remove switch prompts; fix return click and center the brand — 2026-09-22

- Replaced the semantic button with a pointer-only mark target as requested; removed tooltip, dynamic accessibility prompt, pressed state, focus styling and the two now-unused locale labels. The initial sequence still ignores clicks until complete.
- Fixed the failed second click by raising the brand layer and disabling pointer events on the entire hidden model grid, not only each card. A second mark click now changes `brand → models` directly with no automatic sequence. Manual brand remains persistent.
- Reworked brand positioning so automatic/manual full-brand presentation uses `top: 50%` plus two-axis centering within the bordered content area. The initial settle animates from that center to the compact model-stage position; manual transitions animate between the same two coordinates. Slow mark rotation remains unchanged.
- 44 focused model/home tests passed; scoped ESLint, standalone typecheck, isolated Vite build and diff check passed. Logs: `/tmp/sub2api-model-brand-center-tests.log`, `/tmp/sub2api-model-brand-center-lint.log`, `/tmp/sub2api-model-brand-center-typecheck.log`, `/tmp/sub2api-home-model-brand-center-build.log`.
- Prepared/syntax-checked `/tmp/sub2api-home-browser/model-brand-center-check.cjs` for initial completion, persistent brand hold, exact content-center geometry and the second click returning to visible models. Browser/HTTP execution remains unavailable under the recorded sandbox restriction; no live geometry/visual result claimed.
- Preview files refreshed offline from `/tmp/sub2api-home-model-brand-center-dist`, entry `/assets/index-DbumGlJe.js`, publisher `/tmp/sub2api-publish-model-brand-center-preview.py`, backup `/tmp/sub2api-home-pre-model-brand-center-backup`. Cached/current entry and CSS aliases refreshed; imports validated. No restart, production changes, commit or push.


## Address compact ChatGPT mark aliasing with native vector rotation — 2026-09-22

- Replaced the rotating HTML wrapper with a native SVG `g` / `animateTransform` around the existing paths and requested geometric-precision shape rendering. This targets composited-wrapper resampling without changing logo paths, viewBox, displayed size/position, centered manual stage, click behavior, first-visit timing or the 36s rotation period. No raster asset, blur workaround, dependency or JS frame loop.
- Existing visibility preferences now pause/resume the native SVG clock via a component-scoped watcher. The SVG is never remounted or rewound during stage changes. Added regressions for SVG namespace/geometry, duration/center, unchanged node identity, pause/resume and no clock resets.
- All 103 homepage tests passed; scoped ESLint, isolated Vite build (including configured Vue type checker) and diff check passed. Logs: `/tmp/sub2api-model-vector-rotation-tests.log`, `/tmp/sub2api-model-vector-rotation-lint.log`, `/tmp/sub2api-home-model-vector-rotation-build.log`. Full unrelated frontend suite not rerun.
- Prepared/syntax-checked `/tmp/sub2api-home-browser/model-vector-rotation-check.cjs` for native rotation progression, compact screenshot, centered persistent manual brand, return click and offscreen clock pause. Browser/HTTP execution remains unavailable under the previously recorded restriction; no screenshot, antialiasing improvement or live geometry result is claimed.
- Updated only authorized preview files offline from `/tmp/sub2api-home-model-vector-rotation-dist`, entry `/assets/index-DY2t0hqj.js`, publisher `/tmp/sub2api-publish-model-vector-rotation-preview.py`, backup `/tmp/sub2api-home-pre-model-vector-rotation-backup`. Cached/current aliases refreshed, imports validated. No service restart, production/database change, commit or push.


## Compact feature tabs, relocated descriptions and ASTRA galaxy travel — 2026-09-22

- Moved all three approved description paragraphs out of the feature selectors and into the matching scene frame. Selectors now use a compact icon/title/index row, retain the single shared selection/progress system, and remain connected to their own description IDs. Text switches with the scene, with a separate intrinsic text row for speed/trust and a legible lower overlay for ASTRA. No duplicate controls or previously removed marketing headlines.
- Replaced only the intelligence scene with the original SVG `HomeAstraJourney.vue`: a full-bleed dark field, projected spiral galaxy, soft stellar core, dense fine dust, outward accelerating star trails and ASTRA wordmark. Speed/trust SVG art, supported-model art, first screen and header are preserved. Static seeded geometry; 64 CSS flight groups and three batched dust paths; existing visibility/current-scene gates pause all motion. No raster runtime assets, dependency additions, invented service figures or JS frame loop.
- 106 homepage tests passed (11 files), including moved-copy/selection/describedby checks, four new galaxy composition/geometry/ID/portrait tests, and retained scene and model regressions. Full frontend run: 1006 passed and the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription date assertions). Full lint/typecheck passed with the same 12 unrelated warnings; final scoped lint, final isolated build and diff check passed. An initial assertion against a removed scene headline also matched an approved paragraph after relocation; it was narrowed, and the full homepage suite passed again after the final artwork refinement.
- Logs: `/tmp/sub2api-astra-feature-final-tests.log`, `/tmp/sub2api-astra-feature-full-tests.log`, `/tmp/sub2api-astra-feature-lint.log`, `/tmp/sub2api-astra-feature-typecheck.log`, `/tmp/sub2api-astra-feature-final-lint.log`, `/tmp/sub2api-home-astra-feature-final-build.log`.
- Exported the actual standalone Vue/SVG scene using the installed Vue compiler/SSR renderer, then rendered it locally with installed librsvg/cairo (no browser, network or package installation). Visually inspected both `/tmp/sub2api-astra-art-landscape.png` and `/tmp/sub2api-astra-art-portrait.png`; removed hard ribbon-like edges in favor of blurred spiral dust and batched fine particles. Sources `/tmp/sub2api-astra-art-{landscape,portrait}.svg`; helper scripts `/tmp/sub2api-export-astra-svg.cjs` and `/tmp/sub2api-render-static-svg.py`. These are **static artwork renders only**, not page-layout or motion validation.
- Prepared/syntax-checked `/tmp/sub2api-home-browser/astra-feature-check.cjs`: five viewport sizes, compact tabs, moved copy, actual star motion, inactive suspension, desktop single-screen bounds and screenshots. Browser/live HTTP checks remain unavailable under the recorded restriction and were not retried; live animation and one-screen geometry are not claimed as verified.
- Refreshed only authorized preview files from `/tmp/sub2api-home-astra-feature-final-dist`, entry `/assets/index-D_5CGSEX.js`, publisher `/tmp/sub2api-publish-astra-feature-preview.py`, backup `/tmp/sub2api-home-pre-astra-feature-backup`. Cached/current JS/CSS aliases updated and imports checked offline. No service restart, production/container/proxy/database changes, commit or push.

## Infinite first-person galaxy film and staged copy — 2026-09-22

- Removed the lower-right playback control and the old eight-second feature-advancement clock. Feature switching is manual; the active intelligence film loops indefinitely, including after hover/focus. Returning to intelligence remounts both the SVG film and caption layer to restart them in sync.
- Replaced the external spiral view with a centered first-person galactic dust lane, perspective-projected cruise/fast particles, foreground cloud passage and restrained arrival light. A shared 28s infinite CSS clock sequences three bilingual copy passages and the final outlined SVG ASTRA wordmark. Stars continue during the end card and the opening composition returns smoothly; no terminal hold or forced feature switch. Original intelligence description retained as accessible text; speed/trust copy and all other homepage content/layout preserved.
- 105 homepage tests passed (11 files). Added assertions for removed controls/countdown, persistent selected feature across repeated animation events, no hover/focus pause, synchronized replay, native vector geometry, centered portrait/landscape vanishing point, unique SVG references and infinite timeline declarations. Initial test-only file URL reads were incompatible with Vite's transformed import URL; switched to Vite raw-source imports and reran successfully.
- Full frontend suite: 1005 passed, same five unrelated failures in persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date expectations. Full lint: zero errors, 12 existing unrelated warnings. Typecheck, isolated Vite build and diff check passed. Logs: `/tmp/sub2api-astra-film-{tests,full-tests,lint,typecheck,build}.log`.
- Visually inspected offline SVG artwork via installed Vue SSR/librsvg: landscape/portrait plus departure/crossing/beyond shot samples. Softened initial hard cloud edges after inspecting the first render. Helpers: `/tmp/sub2api-astra-static-shot.py`, `/tmp/sub2api-astra-film-frames.py`; representative PNGs `/tmp/sub2api-astra-film-{departure,crossing,beyond,astra}.png`. These sample SVG geometry/exposure only, not browser execution or HTML caption layout. Browser/HTTP checks remain unavailable under the recorded environment restriction and were not retried; frame rate, live loop continuity and actual viewport fit remain unverified.
- Refreshed only the authorized static preview files from `/tmp/sub2api-home-astra-film-dist`, entry `/assets/index-B-oXqgYW.js`, publisher `/tmp/sub2api-publish-astra-film-preview.py`, backup `/tmp/sub2api-home-pre-astra-film-backup`. Cached/current aliases bridged, relative imports checked offline. No service restart or production/container/proxy/database changes. No commit or push.

## Sharper depth cues and tighter ASTRA pacing — 2026-09-22

- Responded to the blurred/sluggish feedback by removing all turbulence/Gaussian filters and foreground clouds, not merely shortening their animations. Kept only faint far-distance light and sharp dust. Replaced flat strokes with tapered gradient trails and brighter crisp heads, weakened the edge vignette and caption scrim.
- Changed flight projection to samples of `s=1/(4-3.875t)`, widened the world cross-section and added a close-flyby layer. At half-cycle the projection is about 0.485 instead of about 0.12; nearby stars traverse the viewport instead of lingering at the center. 300 particles in three layers use 1.1–4.8s staggered cycles. Recycling occurs after leaving the viewport. Cruise is always fully exposed; even the ASTRA ending keeps faster passes visible.
- Halved the shared film/copy loop from 28s to 14s, shortened caption fades and moved ASTRA earlier. No playback button, hover pause, automatic feature change, new dependency, JS frame loop or production change. Other features and page geometry untouched.
- 106 homepage tests passed; final 20 focused scene/carousel tests passed after contrast refinement. Added mathematical projection, cross-section/near-plane, filter absence and fast-layer regressions. Full suite: 1006 passed, the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint/typecheck and isolated build passed; same 12 unrelated lint warnings and existing large-chunk warning. Scoped final lint and diff check passed. Logs: `/tmp/sub2api-astra-crisp-{tests,full-tests,typecheck,lint,final-tests,final-lint,build}.log`.
- Offline artwork helper `/tmp/sub2api-astra-crisp-frames.py` exports representative landscape/portrait SVG shots; checked final crossing PNGs in both layouts. No browser/network workaround attempted. Actual in-browser motion, pacing perception, frame rate and live HTTP health remain unverified.
- Refreshed only authorized preview files from `/tmp/sub2api-home-astra-crisp-dist`, entry `/assets/index-tEX0ACKp.js`, publisher `/tmp/sub2api-publish-astra-crisp-preview.py`, backup `/tmp/sub2api-home-pre-astra-crisp-backup`. Cached/current aliases bridged and imports checked offline. No production lifecycle operation, DB change, commit or push.

## Quiet captions, varied cinematography and render-budget reduction — 2026-09-22

- Replaced centered two-line copy/notes with three short bilingual strings, muted 13–16px type, shot-specific corner placement and subtle directional drift. Removed the full-panel caption scrim and reduced ASTRA's outlined wordmark size. Original accessible feature descriptions, manual feature selection and all unrelated page content remain unchanged.
- Four genuinely different compositions share the existing 14s infinite timeline: offset grazing star field, faster warp from another vanishing point, curved light streams around a dark horizon, then moving diffraction lines and ASTRA. No playback control, frozen ending or automatic feature change.
- Addressed the reported choppiness structurally: 300 independent particle clocks become 12 batched depth-sheet clocks carrying 288 particles. Static stars are three paths instead of hundreds of circles; dust is reduced. Offline SSR geometry measurement: 1,804 SVG elements -> 101 including root (about 94% fewer). No gradient per particle, JS frame loop, blur filter, heavy dependency or mass `will-change` promotion. Other model/slider visibility gates were read-only inspected and preserved.
- 108 homepage tests pass (11 files); final focused regression run 22/22 after smoothing the lens curves. New tests enforce the SVG node budget, twelve-sheet batching, deterministic geometry/phase spacing, perspective projection, distinct shot primitives/focal points, portrait composition, quiet caption structure and continuous end-card streams. Full suite: 1008 passed, the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint/typecheck, final scoped lint, isolated build and diff check pass; unchanged 12 unrelated lint warnings and existing large-chunk warning.
- Logs: `/tmp/sub2api-astra-edit-{tests,full-tests,typecheck,lint,final-tests,final-lint,build,geometry}.log`. Representative native SVG artwork rendered using `/tmp/sub2api-astra-edit-frames.py`; inspected landscape warp/lens and portrait lens, refining initially pointed lens curves into smooth explicit cubic segments. Samples `/tmp/sub2api-astra-edit-{landscape,portrait}-{departure,crossing,beyond,arrival}.{svg,png}`. The SVG budget is measured; browser FPS, compositor behavior, transition pacing, caption positioning and live HTTP health are **not** verified under the existing restriction. No browser/network workaround attempted.
- Refreshed only authorized static preview files from `/tmp/sub2api-home-astra-edit-dist`, entry `/assets/index-DGF4-Ny2.js`, publisher `/tmp/sub2api-publish-astra-edit-preview.py`, backup `/tmp/sub2api-home-pre-astra-edit-backup`. Cached/current aliases bridged and imports checked offline. No production lifecycle action, DB change, commit or push.

## Native intelligence: continuous problem-to-structure illustration — 2026-09-22

- Implemented the approved replacement direction rather than iterating the star field. Renamed/replaced `HomeAstraJourney.vue` with `HomeIntelligenceScene.vue` and updated its consumer/tests. The same code/constraint/dependency input cards, layered system nodes and result card persist through understanding, association and resolution. Secondary connections diminish while an accent route and structured result become clear. No cinematic cuts, particle travel, fake score, typing simulation or purported model-internal transcript.
- Returned the panel to shared homepage light/dark palette tokens with translucent surfaces, fine strokes and shallow layer separation. Brief bilingual margin copy becomes `理解全貌。` / `深入关联。` / `清晰落地。`; the outlined ASTRA signature moves into the lower-right margin. Both illustration and copy share a 14s infinite clock. A route signal travels during the final phase; selection remains manual and hover/focus do not pause it. Original accessible feature paragraphs and other feature/page content remain intact.
- Portrait reflows inputs across the top, system in the middle and result below, with explicit top-entry ports and a continuous right-side output connector. Uses `xMidYMid meet`, not decorative-background cropping. Maintains existing desktop chapter sizing/natural mobile flow. Complete SVG: 83 elements including root; no new dependency, raster/filter work, per-frame JavaScript or random generation.
- 107 homepage tests pass in 11 files. Replaced the old flight tests with six continuous-system regressions covering content primitives, node budget, deterministic geometry, local ID references, portrait reflow/port endpoints/node identity and shared looping timeline; retained feature selection, localized caption, hidden-state and full-page regressions. Full frontend suite: 1007 passed, the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date assertions). Full lint/typecheck and isolated Vite build pass; unchanged 12 unrelated lint warnings and existing chunk-size warning. Diff check passes.
- Logs: `/tmp/sub2api-intelligence-system-{tests,full-tests,lint,typecheck,build}.log`. Offline Vue/SVG helpers `/tmp/sub2api-export-intelligence-svg.cjs` and `/tmp/sub2api-intelligence-static.py`; inspected light landscape association/resolution, portrait resolution and dark landscape resolution PNGs. Static samples resolve theme fallback values and representative phase attributes; they are not browser execution, caption-layout or FPS verification. Live HTTP/browser checks remain unavailable and were not retried or worked around.
- Updated only authorized static preview files from `/tmp/sub2api-home-intelligence-system-dist`, entry `/assets/index-CqpD_725.js`, publisher `/tmp/sub2api-publish-intelligence-system-preview.py`, backup `/tmp/sub2api-home-pre-intelligence-system-backup`. Cached/current aliases bridged; imports checked offline. No service restart, production/container/proxy/DB change, commit or push.

## Numbered intelligence captions — 2026-09-22

- Added `1.`, `2.`, `3.` to the three intelligence captions and removed terminal punctuation, in Chinese and English. No animation, layout or service changes.
- 39 focused carousel/homepage tests, scoped ESLint, isolated Vite build and diff check pass. Logs: `/tmp/sub2api-intelligence-numbered-{tests,lint,build}.log`. The unrelated full suite was not rerun for this copy-only change.
- Refreshed authorized preview files offline from `/tmp/sub2api-home-intelligence-numbered-dist`, entry `/assets/index-DpK2i_Rd.js`, publisher `/tmp/sub2api-publish-intelligence-numbered-preview.py`, backup `/tmp/sub2api-home-pre-intelligence-numbered-backup`. Import/alias checks pass; browser/HTTP verification remains unavailable. No production lifecycle action, commit or push.

## Restore original visible feature descriptions — 2026-09-22

- Corrected the intelligence paragraph's unintended screen-reader-only treatment. Its complete original localized text is visible again in a dedicated row below the artwork; speed/trust descriptions remain unchanged and visible in their selected panels. Numbered phase captions are supplemental, not substitutes, and no long description is moved back into a selector.
- Moved the timed caption/signature wrapper inside `.feature-art` so it cannot overlay the restored paragraph. Replaced the intelligence-only absolute paragraph rule with a padded intrinsic footer and separator. Artwork/text now occupy flexible/auto grid rows, with a smaller desktop artwork minimum to reserve readable text space and natural mobile sizing. Scene geometry, clock, numbering and original copy are unchanged.
- 108 homepage tests pass, including a regression selecting all three features and asserting exact original copy in a visible, non-screen-reader-only sibling below the art; the animation overlay stays inside the art. Full suite: 1008 passed, same five unrelated failures in persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date assertions. Full lint/typecheck, isolated build and diff check pass; existing 12 unrelated lint warnings/chunk warning remain. Logs: `/tmp/sub2api-home-feature-descriptions-{tests,full-tests,lint,typecheck,build}.log`.
- Refreshed authorized static preview files from `/tmp/sub2api-home-feature-descriptions-dist`, entry `/assets/index-arb3wLo4.js`, publisher `/tmp/sub2api-publish-feature-descriptions-preview.py`, backup `/tmp/sub2api-home-pre-feature-descriptions-backup`. Cached/current aliases and imports verified offline. Browser layout/viewport fit and live HTTP health remain unverified under the existing restriction. No production lifecycle action, DB change, commit or push.

## Unified right-aligned feature footers — 2026-09-22

- Applied the intelligence panel's artwork/copy separation to all three features: thin top divider, dedicated intrinsic footer and right-aligned complete description. No copy, animation, numbering, selector or chapter-layout change.
- 40 focused carousel/homepage tests, scoped ESLint, isolated build and diff check pass. Logs: `/tmp/sub2api-home-all-descriptions-right-{tests,lint,build}.log`.
- Refreshed authorized preview files from `/tmp/sub2api-home-all-descriptions-right-dist`, entry `/assets/index-BJphfetP.js`, publisher `/tmp/sub2api-publish-all-descriptions-right-preview.py`, backup `/tmp/sub2api-home-pre-all-descriptions-right-backup`. Browser/live HTTP checks remain unavailable. No production lifecycle action, commit or push.

## Compact desktop footer and connected intelligence focus — 2026-09-22

- Removed the description's 820px cap while retaining every approved word, divider and right alignment across all tabs. Desktop text uses the whole row, a bounded 13–14px size and 1.7 line height; intelligence-footer vertical padding is 12px. Natural wrapping remains for narrow screens, zoom and translations, without ellipsis, fixed text height or forced nowrap. Added desktop width-budget checks for the 60-character Chinese paragraph at 1024/1080/1280/1440/1920px; these are arithmetic regressions, not browser layout verification.
- Fixed the animation's geometry rather than masking the gaps: wired cards and network planes no longer independently translate or scale, and the desktop output port is exactly aligned to the core's y=250 axis. One uninterrupted root-space path and its matching travelling signal now extend from the input edge through the core to the result port. The portrait route shares its actual input/output connectors, too. Background links stay drawn at low contrast; removed distracting diagonal cross-connections and the opaque core tile. Input outline -> central halo -> result outline/check establish sequential focus. Only the non-wired halo scales. Existing copy, numbered captions, infinite 14s loop, manual tab selection, suspension and other SVG scenes are preserved.
- 56 focused tests and all 112 homepage tests pass. Added input/output attachment, single-subpath/shared-coordinate, visible-center-route, fixed-geometry and full-width text regressions. Full frontend suite: 1012 pass and the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date assertions). Full lint/typecheck pass with the same 12 unrelated lint warnings. The initial concurrent build terminated with exit 143; a subsequent standalone isolated build passed in 32.71s with the existing chunk-size warning. Diff check passes.
- Logs: `/tmp/sub2api-home-connected-focus-{tests,full-tests,lint,typecheck,build,build-retry}.log`. Offline Vue/librsvg samples `/tmp/sub2api-connected-{landscape,portrait}-{understand,connect,resolve}.{svg,png}` and `/tmp/sub2api-connected-dark-resolve.{svg,png}` inspect composition and connected geometry, not live CSS playback. Measured 92 SVG elements including root, no added filters, dependencies, raster UI assets or JS frame loop. Browser motion, footer geometry, FPS and live HTTP health remain unverified; no restricted-browser/network workaround attempted.
- Refreshed only authorized preview files from `/tmp/sub2api-home-connected-focus-dist`, entry `/assets/index-DE1uVZpO.js`, publisher `/tmp/sub2api-publish-connected-focus-preview.py`, backup `/tmp/sub2api-home-pre-connected-focus-backup`. Cached/current aliases and local imports validated; backend embedded index checksum unchanged. No production/container/proxy/database change, service restart, commit or push.

## Living intelligence: progressive associations and alternatives — 2026-09-22

- Responded to the overly static/simple diagram without reintroducing independently moving wired cards. Input contents write in with staggered outlines and three travelling source streams. Three cubic association paths share actual network junctions; 12 additional small nodes and four bridge paths add depth, while offset local signals and node accents create activity inside the association phase. A restrained local echo and segmented core ring respond without shifting connection points.
- Two abstract structural alternatives temporarily occupy the result area during association. They are linked to the same fixed system output port, with responsive edge attachments and bidirectional signal motion, then recede as the final structured result appears. Result/primary-route reveal is delayed until the second half of the sequence instead of showing the whole answer at the beginning. The main 14s cycle, numbered captions, final travelling highlight, original text, compact right-aligned footer and all other homepage features remain intact. The illustration is conceptual, not an alleged model reasoning trace or fabricated benchmark.
- 59 focused tests and all 115 homepage tests pass. New/updated regressions cover the bounded 138-element SVG, multiple continuous shared-junction paths, unique additional nodes, delayed result reveal, main-phase/local-rhythm separation, three connected input streams, and portrait/landscape candidate edge attachments. Full suite: 1015 pass, same five unrelated failures in persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date assertions. Full lint/typecheck pass with 12 existing unrelated lint warnings. Initial isolated build exited 143 without a diagnostic; normal retry passed in 32.96s with the existing chunk warning. Diff check passes.
- Logs: `/tmp/sub2api-home-living-intelligence-{tests,full-tests,lint,typecheck,build,build-retry}.log`. Static sampler `/tmp/sub2api-intelligence-rich-samples.py`; inspected `/tmp/sub2api-living-{landscape,portrait}-{understand,connect,resolve}.{svg,png}` and dark association sample `/tmp/sub2api-living-dark-connect.{svg,png}`. These are representative native SVG artwork samples, not browser CSS playback or page-layout verification. No filters, raster UI assets, new dependencies or frame-level JS. Browser animation/FPS/HTTP health remain unverified; no network or restricted-browser workaround attempted.
- Updated only authorized preview files from `/var/tmp/sub2api-home-living-intelligence-dist`, entry `/assets/index-BWzW886I.js`, publisher `/tmp/sub2api-publish-living-intelligence-preview.py`, backup `/var/tmp/sub2api-home-pre-living-intelligence-backup`. Cached/current entry aliases, local imports and published homepage bytes checked; backend embedded index checksum unchanged. No production lifecycle action, database change, restart, commit or push.

## Speed: global geographic routes and a fast response — 2026-09-22

- Replaced only the second feature's transport-tunnel artwork with `HomeSpeedScene.vue`. The themed orthographic globe uses the already-present Natural Earth coastline asset and `d3-geo`; seven illustrative origins connect along raised great-circle paths to a common hub. Three optimized lanes carry rapid packet highlights to a response console and return acknowledgement highlights over the identical geometry. First-byte illumination precedes quick response-row and token arrival in a 3.2s loop. No extra marketing text, numeric counters, fabricated live telemetry or claimed operating-node locations; original copy/selector/footer and intelligence scene are untouched.
- Static geometry lives in `speedGlobe.ts`, computed once rather than per frame. Projection, endpoint radius, front-hemisphere visibility and common-hub attachment are mathematically checked. Globe position/ports never rotate as a flat layer. Portrait recomputes vertical lanes and console top-edge endpoints while retaining the full globe. Final lane joins use tangent-aligned cubics rather than angular quadratic-to-straight joins. Removed unreachable old speed markup/styles from `HomeFeatureScene.vue`; verified every trust drawing primitive is unchanged (whitespace-normalized comparison to `/var/tmp/sub2api-speed-redesign-feature-scene-before.vue`).
- 54 initial focused tests and all 122 homepage tests pass. Added seven speed tests covering geography/route geometry, three-lane attachment in both layouts, deterministic responsive globe identity, unique/local SVG references, bounded SVG size, early response ordering and CSS-only motion. Full frontend suite: 1022 pass and the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription dates). Full lint/typecheck pass with the same 12 unrelated lint warnings. After the final curve refinement, 24 focused tests, scoped lint and a fresh isolated build pass (29.99s, existing large-chunk warning). Diff check passes.
- Logs: `/tmp/sub2api-home-global-speed-{tests,full-tests,lint,typecheck,build,final-tests,final-lint,final-build}.log`. Offline helpers `/var/tmp/sub2api-speed-artwork/export.cjs` and `render.py`; inspected landscape/portrait transit/response and dark response SVG/PNG artwork in the same directory. Measured 130 SVG elements including root; no bitmap UI assets, filters, dependencies, runtime network or frame-level JS added. These are native vector composition checks, not browser playback, FPS or final page-geometry verification. Browser/HTTP health remains unverified under the environment restriction; no workaround attempted.
- Refreshed only authorized static preview files from `/var/tmp/sub2api-home-global-speed-dist`, entry `/assets/index-7Dr2j_Cb.js`, publisher `/tmp/sub2api-publish-global-speed-preview.py`, backup `/var/tmp/sub2api-home-pre-global-speed-backup`. Cached/current aliases, imports and homepage bytes checked offline; backend embedded index checksum unchanged. No production lifecycle operation, DB/proxy/container change, service restart, commit or push.

## Speed: globe on the right, synchronized request/response — 2026-09-22

- Moved the desktop globe right and client left; portrait now places the client above the full globe. Reversed/recomputed the three lane paths so they start at their actual client ports and end at the projected hub, retaining continuous tangent-aligned curves. Reduced decorative track contrast and limited animation to one selected lane and geographic route instead of seven simultaneous streams and three independent return paths.
- All motion shares one 5.6s clock: request input -> accelerating send -> hub -> target echo -> target/hub return -> client return -> first byte -> streamed result. Directional chevrons, light-only halo breathing, arrival echoes and a cursor add small responsive details without shifting wired geometry. Delayed rows/tokens have backwards fill to avoid initial flashes. Other feature scenes, original descriptions, dividers, layout and the rest of the homepage are unchanged.
- All 122 homepage tests pass. Speed regressions cover right/left and portrait order, exact client-to-hub attachment, one active route/lane, a single shared animation period and explicit non-competing phase handoffs. Replaced the outdated generic minimum-path-count assertion with semantic scene checks; fewer duplicate paths is intentional. Full frontend suite: 1022 pass with the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date assertions). Full lint/typecheck pass with 12 existing unrelated warnings; final scoped lint passes. Isolated build passes in 38.93s with the existing large-chunk warning. Diff check passes.
- Logs: `/tmp/sub2api-home-speed-roundtrip-{tests,full-tests,lint,typecheck,final-lint,build}.log`. Offline export/sampler and prepare/send/target/return/response artwork are under `/var/tmp/sub2api-speed-roundtrip-artwork/`; inspected landscape send/response, portrait response and dark response PNGs. Measured 118 SVG elements. These are representative native vector samples, not browser playback/layout/FPS checks. Live HTTP health remains unverified; no restricted-browser/network workaround attempted.
- Refreshed only authorized preview files from `/var/tmp/sub2api-home-speed-roundtrip-dist`, entry `/assets/index-BPga8Zw4.js`, publisher `/tmp/sub2api-publish-speed-roundtrip-preview.py`, backup `/var/tmp/sub2api-home-pre-speed-roundtrip-backup`. Checked free space before publication; cached/current JS aliases, cached CSS, local imports, disk index and homepage JS/CSS bytes verified offline. Backend embedded index checksum unchanged. No production lifecycle operation, DB/proxy/container change, service restart, commit or push.

## Speed: Netherlands-only destination, global traffic and Pi terminal — 2026-09-22

- Corrected the server marker to a Netherlands country-level coordinate `[5.3, 52.2]` based on the owner's clarification. The foreground exchange now goes client -> Netherlands -> client, with no third-node relay. All seven geographic client origins have independently staggered, lower-contrast request/return highlights again; every path pair shares the same geometry and Netherlands destination. One labeled server glyph replaces the anonymous hub. The globe remains on the right, with full portrait recomposition below the client.
- Rebuilt the client as a wider Pi-style terminal: monochrome native Pi mark, transcript above a bottom editor, working directory and ASTRA footer. Reviewed the locally installed Pi 0.84.3 README and packaged interactive-mode image after the web reference request returned 403; did not attempt a network workaround. The screenshot is a reference only, not a shipped asset. Small localized SVG text uses system monospace fonts. Original marketing text, footers, intelligence/trust components, carousel and HomeView are unchanged (component checksums verified).
- The main 6.4s CSS loop explicitly orders direct request delivery, return/first byte, thinking status, then sequential reply typing; each clipped reply line completes before the next starts. Continuous global round trips use a separate 3.6s clock and remain active while the client thinks/outputs. No fabricated reasoning, live usage figures, runtime network calls, dependencies, filters, raster assets or frame-level JavaScript were added. Existing pause/reduced-motion behavior applies; static mode shows the completed reply instead of stuck thinking.
- All 59 initial focused tests and all 127 final homepage tests pass. Regressions cover Netherlands coordinates and exact server/route/port attachment, seven active direct route pairs, bilingual received/thinking/reply labels, Pi transcript-editor-footer ordering in both layouts, non-overlapping text rows and first-byte-before-thinking-before-reply timing. Full suite: 1027 pass, same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date assertions). Full lint/typecheck pass with 12 existing unrelated lint warnings. Isolated build passes in 37.37s with the existing large-chunk warning. Diff check passes.
- All current logs, before snapshots, component/backend checksums, native SVG exporter, static sampler and phase artwork are in `/var/tmp/sub2api-speed-netherlands-artwork/` (`focused-tests.log`, `home-tests.log`, `full-tests.log`, `lint.log`, `typecheck.log`, `build.log`). Inspected Chinese landscape thinking/reply, portrait reply, dark reply and English landscape reply samples. Measured 152 SVG elements. These are offline vector composition checks, not browser CSS playback, FPS or page-layout/HTTP verification; those remain unverified under the environment restriction.
- Updated only authorized static preview files from `/var/tmp/sub2api-home-speed-netherlands-dist`, entry `/assets/index-wVMp-AZX.js`. Publisher: `/var/tmp/sub2api-speed-netherlands-artwork/publish-preview.py`; backup: `/var/tmp/sub2api-home-pre-speed-netherlands-backup`. Free space checked before copying; imports, cached/current entry aliases, cached CSS, disk index, homepage assets and locale chunks verified by local byte equality. Backend embedded index unchanged. No production/container/proxy/database operation, service restart, commit or push.

## Unbranded streaming and a longer task — 2026-09-22

- Addressed both user requests: renamed the Chinese title to `快速响应 · Speed`; replaced country/rack labeling with a plain `Server` callout and geographic point, and removed all client brand marks, names, example conversation text and status wording. Removed the seven unused speed-scene strings from each locale. The original service description, Netherlands endpoint, global routes, other feature scenes and page layout are unchanged; their component/geometry checksums match.
- Replaced the text typewriter with two actual illustrative packet trains: 18 highlights per batch, each paired by index and delay with a corresponding growing output segment. A received segment appears at the same normalized time its packet reaches the client, before later packets finish travelling. The 16s task includes processing dots, the first stream, a three-step tool/progress graphic, an outgoing follow-up and second stream, transcript scrolling and completion. The editor/window remain fixed; seven quieter global round trips continue throughout. No real agent task is executed or measured.
- Fixed the input's discontinuous handoff: the old 6.4s timing left only 0.128s between complete input and disappearance, then displayed a separate transcript copy. The new single graphic input holds for 0.64s and moves into history while remaining visible. Explicit hidden initial reveal/opacity states and invisible loop reset avoid the old duplicate/opacity swap; there is no input text or font dependency. Live browser flicker/playback has not been measured in this restricted environment.
- 60 focused tests and all 128 homepage tests pass. New regressions enforce Server-only SVG text, no product/rack glyph, single input identity and continuous handoff, 36 one-to-one packet/output pairs, both streaming windows and tool timing, clipped portrait/landscape long-task geometry and completed reduced-motion output. Full suite: 1028 pass, same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint/typecheck pass with 12 existing unrelated warnings. Isolated build passes in 39.29s with the existing large-chunk warning. Diff check passes.
- Current logs, source snapshot, checksums, offline exporter/sampler and phase SVG/PNG files: `/var/tmp/sub2api-speed-longtask-artwork/` (`focused-tests.log`, `home-tests.log`, `full-tests.log`, `lint.log`, `typecheck.log`, `build.log`). Inspected landscape input/first stream/tool phase, portrait completion and dark second stream. Measured 272 simple SVG elements; no filters, raster assets, new dependencies, runtime network or frame-level JS. Browser playback/layout/FPS and HTTP health remain unverified; no restricted-browser/network workaround attempted.
- Updated only authorized preview files from `/var/tmp/sub2api-home-speed-longtask-dist`, entry `/assets/index-dvlI3-MN.js`. Publisher `/var/tmp/sub2api-speed-longtask-artwork/publish-preview.py`, backup `/var/tmp/sub2api-home-pre-speed-longtask-backup`. Free space, local imports, cached/current JS aliases, cached CSS, disk index, homepage assets and locale bytes verified. Backend embedded index and untouched homepage component/geometry checksums match. No production lifecycle operation, database/proxy/container change, restart, commit or push.


## Continuous output connection and no bottom client meter — 2026-09-22

- Replaced all 36 flying output packets with one full solid path sharing the main connection geometry. It stays steadily visible during both output windows and fades between phases; no stream dash pattern, per-packet delay or flight keyframe remains. Client output still accumulates and scrolls through the existing 16s long task; all seven geographic round trips and the input handoff are unchanged.
- Removed the left client's bottom progress track/fill/completion tick, unused footer coordinate and corresponding keyframes. Preserved tool-card progress, client size, editor position, original descriptions and all other scenes. Updated regressions enforce one continuous response path, full timing coverage and absence of the footer in both layouts. The timing test caught the first plateau ending 60ms before its last piece finished; extending it from 40% to 41% resolved this before publication.
- All 128 homepage tests pass. Final full suite: 1028 passed, the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint/typecheck pass with 12 existing unrelated lint warnings. Isolated build passes in 30.40s with the existing large-chunk warning; diff check passes. Logs and before-source/checksum records: `/var/tmp/sub2api-speed-continuous-artwork/` (`home-tests.log`, `full-tests-final.log`, `lint.log`, `typecheck.log`, `build.log`).
- Offline landscape first-stream and portrait second-stream compositions inspected, showing the solid connection and no bottom meter; internal tool progress remains. Measured 233 SVG elements, down from 272. No dependencies, filters, raster UI assets, runtime network or frame-level JS added. Browser playback/FPS/layout and live HTTP health remain unverified; no restricted-network/browser workaround attempted.
- Updated only authorized preview files from `/var/tmp/sub2api-home-speed-continuous-dist`, entry `/assets/index-DrkkDYnR.js` (HomeView JS `HomeView-CnW3d-A9.js`, CSS `HomeView-GTjlBpAQ.css`). Publisher and publication log in the artifact directory; backup `/var/tmp/sub2api-home-pre-speed-continuous-backup`. Free space, local imports, all new asset bytes, disk index, cached/current JS aliases and cached CSS checked offline. Backend embedded index and untouched homepage/locale checksums match. No production lifecycle action, DB/proxy/container change, restart, commit or push.


## Flowing response body instead of whole-line illumination — 2026-09-22

- Replaced the opacity-only stream with a directional, single-body SVG mask. The front extends from Server to client before output begins; the connection stays full during streaming, then its tail drains toward the client near the final output segment. Both batches use the existing 16s clock. A 100-unit body / 200-unit gap advances monotonically across the client-to-server path in the opposite direction, with no visible backwards reset or separate flying packets.
- Added a repeating monochrome gradient moving beneath the mask: left on landscape, up on portrait. All gradient stops retain at least .5 opacity and the first/last stops match; a one-period translation and oversized surface rectangle prevent gaps or seams. A narrow center reflection adds depth without filters. The mask matches the existing primary lane and uses component-local IDs. Client output, long-task/tool sequence, global routes, geography, original text, other scenes and removed footer meter stay unchanged.
- 15 speed tests and all 130 homepage tests pass. Regressions derive visible front/body/tail samples from actual CSS keyframes, verify no inter-batch reverse reveal, align output with arrival/drain timing and check local mask IDs, nonzero gradient coverage and both layout directions. Full frontend suite: 1030 passed, same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint passes with 12 existing unrelated warnings. Initial concurrent typecheck exited 143; standalone retry passes. Isolated build passes in 32.05s with the existing large-chunk warning. Diff check passes.
- Artifacts/logs: `/var/tmp/sub2api-speed-fluid-artwork/` (`focused-tests.log`, `home-tests.log`, `full-tests.log`, `lint.log`, `typecheck.log`, `typecheck-retry.log`, `build.log`, offline export/render scripts, source snapshots and checksums). Inspected landscape leading edge and sustained stream plus portrait draining tail, rendered with native SVG/librsvg. Measured 243 SVG elements; no dependencies, filters, raster UI assets, runtime network or JS frame loop. CSS motion inherits existing visibility suspension and reduced-motion fallback. Browser playback/FPS/layout and live HTTP health remain unverified; no restricted-browser/network workaround attempted.
- Updated only authorized static preview files from `/var/tmp/sub2api-home-speed-fluid-dist`, entry `/assets/index-CeT3JB79.js` (HomeView JS `HomeView-CQ9JoXV7.js`, CSS `HomeView-CMVcWLFU.css`). Publisher/log in the artifact directory; backup `/var/tmp/sub2api-home-pre-speed-fluid-backup`. Free-space, local imports, asset bytes, disk index, current/cached JS aliases and cached CSS verified offline. Backend embedded index and untouched homepage/locale checksums match. No production lifecycle action, database/proxy/container change, restart, commit or push.


## Slower illustrative model-generation pace — 2026-09-22

- Slowed the continuous surface period from 1.2s to 3.2s, about 37.5% of its former movement speed. Leading-edge travel is now 0.8s / 0.64s instead of 0.4s / 0.32s; draining tails also last 0.8s / 0.64s. Client fragment spacing is 155ms instead of 140ms, with 120ms growth rather than 80ms. The second response batch is delayed to 62% so output still begins only on arrival. Both final fragments finish before their respective tails reach the client. Extended supporting receive-cue/highlight timing to match.
- Preserved the original request, first-byte acknowledgment, global round trips, input handoff, scrolling, tool work and 16s loop. Compared the unchanged SVG template and request/first-byte/global/input/scroll keyframes against the before snapshot. Other scene, geography, page and locale checksums match. No new copy, telemetry, geometry, dependencies, images, controls or runtime configuration; source comments clarify that generation timings are illustrative, not measurements. The bottom client meter remains removed.
- All 130 homepage tests pass, including 15 speed tests for directional body/tail behavior, revised timing, continuous coverage and unchanged fast request/receipt. Full frontend suite: 1030 pass with the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint/typecheck pass with 12 existing unrelated warnings. Isolated build passes in 34.96s with the existing large-chunk warning. Diff check passes. Logs/snapshots/checksums: `/var/tmp/sub2api-speed-model-pace-artwork/` (`home-tests.log`, `full-tests.log`, `lint.log`, `typecheck.log`, `build.log`). This timing-only change did not require new composition renders; browser playback/FPS and live HTTP health remain unverified, without restricted-network/browser workarounds.
- Updated only authorized static preview files from `/var/tmp/sub2api-home-speed-model-pace-dist`, entry `/assets/index-DlHpkNhg.js` (HomeView JS `HomeView-DMpD1WTR.js`, CSS `HomeView-Ce3QIv89.css`). Publisher and publication log in the artifact directory; backup `/var/tmp/sub2api-home-pre-speed-model-pace-backup`. Free space, local imports, asset bytes, disk index, cached/current entry aliases and cached CSS verified offline. Backend embedded index unchanged. No production lifecycle action, database/proxy/container change, restart, commit or push.


### 2026-09-22 — Trust animation / transparent token billing

- Replaced the old abstract trust artwork with `HomeTrustScene.vue` and the small, derived `trustBillingDemo.ts` fixture. One 16s native SVG/CSS loop shows disjoint normal-input/cache-read/cache-write/output quantities, per-million model prices, line-item costs, pre-rate sum, a single effective multiplier and itemized settlement. Input cache quantities are excluded from normal input; user-specific rate overrides the group rate; actual cost either deducts balance or consumes subscription quota, never both. Standard-tier Terra values are explicitly illustrative, not current pricing or real account data. Configuration-dependent context/service tiers and cache TTL are acknowledged. Both locales and a screen-reader summary are included. Original right-aligned copy and all other feature/section geometry remain unchanged.
- Read-only code references: `backend/internal/service/openai_gateway_service.go` RecordUsage, `billing_service.go` computeTokenBreakdown/computeCacheCreationCost, `model_pricing_resolver.go`, `pricing_service.go`, `user_group_rate_resolver.go`, and `gateway_service.go` buildUsageBillingCommand. Fixture math: 12,000 total input = 4,000 ordinary + 6,400 cache read + 1,600 cache write; 800 output; $0.028600 subtotal × 0.5 = $0.014300. Rates are example standard Terra fallback values, not fetched production settings.
- Final homepage tests: 139/139 across 13 files, including nine new trust tests for arithmetic, disjoint caches, per-million units, proportional meters, explicit example/rule copy, bilingual rendering, contained portrait layout, unique SVG resources and ordered/pause-compatible looping. Final full suite: 1039 pass, five previously documented unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription dates); 150 files. Full lint: zero errors / 12 existing unrelated warnings; final touched-file lint clean. Typecheck passes. Isolated Vite build passes in 31.91s with the existing large-chunk warning. Diff check passes.
- Offline Vue SSR + installed librsvg samples: 118 elements; inspected Chinese/English desktop and portrait, dark theme and intermediate pricing phase. Desktop 1200×520; mobile genuinely recomposed into 480×720 rather than shrinking a wide table. Artifact directory: `/var/tmp/sub2api-trust-billing-artwork/` (source snapshots, export.cjs, render.py, SVG/PNG samples, checksums and test/typecheck/lint/build logs). These are native-vector composition samples, NOT a browser animation/FPS or HTTP check. No restricted-browser/network workaround was attempted.
- Updated only the authorized isolated static preview from `/var/tmp/sub2api-home-trust-billing-dist`. Entry `/assets/index-DlbEvyOV.js`; HomeView `HomeView-Dz7T1D71.js` / `HomeView-DGfffAHA.css`. Backup `/var/tmp/sub2api-home-pre-trust-billing-backup`; publisher and offline verifier in the artifact directory. All 160 built assets, disk index, local imports, cached/current entry bridges and cached CSS verified. The cached startup `index-CXxW4uoe.js` and former `index-DlHpkNhg.js` bridge directly to the new entry. Backend embedded index and other homepage/backend source checksums unchanged. Other locale strings and carousel CSS/logic matched their before snapshots exactly.
- Preview publication added about 4.29 MB, leaving only ~9.2 MB free in `/tmp`; subsequent build/render/log artifacts must stay in `/var/tmp`, and future preview refreshes must check capacity before copying. No production deployment, service/container/proxy/database change, restart, commit or push. Browser playback and live HTTP health remain unverified.


### 2026-09-22 — Wordless, simplified trust interaction

- Replaced the literal pricing table with a 64-element abstract SVG (previously 118): a small usage tile cluster branches into direct/cache paths, a cached tile echoes, both paths converge before one weighting lens, then one result fills a graphical record and a confirmation check. All visible words, numerals, prices, model names, formula labels and illustration footnotes are gone. Deleted the unused `trustBillingDemo.ts` fixture and its detailed locale strings. Only a short bilingual screen-reader summary remains; the original tab headings and right-aligned description/divider are untouched.
- One 10s CSS loop with ordered transfers, gathering and confirmation; desktop 1200×520 / recomposed portrait 600×720. Source ports, cache arcs, shared junction, lens boundary and receipt boundary connect explicitly. No new controls, fonts, raster assets, filters, frame JavaScript, animation timers, dependencies or live API calls. Existing inactive/offscreen/document-hidden suspension and static reduced-motion state remain.
- Final homepage tests: 138/138 across 13 files, including eight abstract-trust tests for zero SVG text, lower element count, connected branches and single settlement, ordered animation phases, stable responsive nodes, local resources and parent visibility handling. Full suite: 1038 pass, five known unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates); 150 files. The initial full run was interrupted when the user continued; preserved in `full-tests-interrupted.log`, then rerun to completion. Full lint: zero errors / 12 existing unrelated warnings; typecheck passes. Isolated Vite build passes in 30.17s with existing chunk-size warning. Diff check passes.
- Inspected offline Vue SSR/librsvg desktop and portrait samples for branching/reuse/completed record, plus dark theme. Sampled path geometry stays within both viewBoxes. These are native-vector composition/phase samples, not browser playback/FPS or HTTP health proof. All artifacts/logs/snapshots/scripts in `/var/tmp/sub2api-trust-abstract-artwork/`. No restricted-browser/network workaround was attempted.
- Updated only the authorized static preview from `/var/tmp/sub2api-home-trust-abstract-dist`: entry `/assets/index-skf4xmBx.js`; HomeView `HomeView-CdNFjXyU.js` / `HomeView-BY-fLXi8.css`. Backup `/var/tmp/sub2api-home-pre-trust-abstract-backup`. All 160 assets, local imports, disk index, cached/current entry aliases and cached CSS verified offline. Startup `index-CXxW4uoe.js` and former `index-DlbEvyOV.js` bridge directly to the new entry. Backend embedded index, other homepage components/view and inspected billing sources match before checksums. Other locale copy is unchanged.
- To keep the existing 8 MiB publication headroom on the nearly-full `/tmp`, relocated ONLY seven documented, non-served historical homepage preview backup payloads to `/var/tmp/sub2api-home-trust-abstract-backup-archive/`: screen-fit, billing-policy, client-links-icon, primary-button, request-display, quick-guide-layout and feature-descriptions. All 35 files were copied and verified byte-for-byte before atomic replacement at their original paths with symlinks. Original paths remain readable with identical SHA-256 hashes; manifest and relocation log retained. No backups were discarded, no active preview asset was relocated and no general temp cleanup occurred. `/tmp` remains near capacity (~9.0 MB free); keep builds/artwork/logs in `/var/tmp` and check preview copy capacity.
- No production lifecycle action, service restart, proxy/container/database changes, commit or push. Live browser playback and HTTP health remain unverified.


### 2026-09-22 — Recognizable request-to-receipt trust story

- Replaced the rejected abstract node/strand illustration with a request window and a printed, serrated receipt. Input, cache and output have matching icon/token fingerprints in the source, moving slip and receipt row. Cached pieces physically move out of the normal-input line into a separate source group; no duplicate remains in ordinary input. Model marks match across the two objects. Coin pictograms convey item pricing, a rate tag next to the combined total applies one adjustment, and a final check confirms the record. There is no visible prose, numeric price, model name, metric or live quote; all token/bar sizes and coin heights are illustrative.
- Native 12s CSS/SVG loop: cache separation/output formation → paper reveal → three entry transfers → line-item cost reveal → one total adjustment → confirmation. The paper is clipped rather than stretched; transfer endpoints match receipt coordinates exactly. Fixed translation groups wrap animated local groups, including the pricing pictograms, to avoid CSS transforms overriding SVG layout anchors. 127 SVG elements; 1200×520 desktop and 600×720 portrait with stable node identity. Existing feature headings, original description/divider, other scenes and all section layout remain unchanged. No runtime timers, frame JavaScript, fonts, raster assets, filters, new controls/dependencies or account calls.
- Final homepage suite: 140/140 across 13 files, including 10 trust tests for recognizable wordless objects, matching fingerprints, non-duplicated cache grouping, single settlement, desktop/mobile transfer coordinates and bounds, nested transform safety, local resources, ordered timeline and motion suspension. Full frontend suite: 1040 pass, five previously documented unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates), 150 files. Full lint: zero errors / 12 existing unrelated warnings. Typecheck passes. Isolated Vite build passes in 28.95s with the existing large-chunk warning. Diff check passes.
- Offline Vue SSR/librsvg samples cover request, cache separation, paper reveal, transfer, pricing, rate adjustment, confirmation and dark theme. Inspected final desktop/mobile and transfer samples; the rate tag is placed by the total rather than beside the output row. Artifacts, snapshots, export/render scripts and logs: `/var/tmp/sub2api-trust-receipt-artwork/`. These samples are not live browser playback/FPS or HTTP health proof; no restricted-network/browser workaround was attempted.
- Authorized preview files updated from `/var/tmp/sub2api-home-trust-receipt-dist`: entry `/assets/index-hvoPdePO.js`; HomeView `HomeView-A8nCtJHA.js` / `HomeView-_Nh8Whr7.css`. Backup `/var/tmp/sub2api-home-pre-trust-receipt-backup`. Verified all 160 assets, local imports, disk index, cached/current JS aliases and cached CSS offline. Startup `index-CXxW4uoe.js` and former `index-skf4xmBx.js` bridge directly to the new entry. Backend embedded index, other homepage components/view and billing-source checksums match their before snapshots. Only the short screen-reader billing summary changed in each locale.
- Preserved publication headroom by archiving ONLY eight more documented, non-served historical preview backup payloads under `/var/tmp/sub2api-home-trust-receipt-backup-archive/`: connected-focus, intelligence-numbered, intelligence-system, astra-film, pricing-gap, pricing-vertical, model-brand-center and model-brand-toggle. All 40 files were copied and hash-verified before atomic replacement with symlinks at the original paths; paths remain readable with identical SHA-256 hashes. Manifest and relocation log retained. No backup loss, active preview asset relocation or general temporary-file cleanup. `/tmp` still has only ~9.4 MB free, so new artifacts stay in `/var/tmp` and subsequent publications must check capacity.
- No production deployment/lifecycle action, restart, proxy/container/database change, commit or push. Live browser playback and HTTP health remain unverified.

### 2026-09-22 — Connected printer and line-by-line receipt

- Removed all airborne usage slips. Each source row has a continuous branch leading into one fixed cable, which physically terminates at the printer inlet. Three travelling highlights follow their exact visible branch/cable geometry, not independent flight paths. Source highlighting no longer overlaps adjacent rows. Desktop connects to the left inlet; portrait routes around both objects into the right inlet, with every control point inside the viewport.
- Replaced the whole-sheet reveal with actual unscaled paper translation through a stationary slot. Header first, then three 62px feeds with readable pauses, then the total. Each signal arrives before its matching feed; one local ink clip prints the icon, usage and pricing pictogram together. Later rows stay hidden inside the printer. Rate adjustment and confirmation remain last and fade with the paper before an invisible reset; ink does not retract while the paper is visible. Cached usage remains separate, the combined total is adjusted once, and no visible prices/prose or fabricated telemetry were introduced.
- 132 SVG elements, one 12s native CSS clock, no JS frame loop/timers, filters, fonts, assets, dependencies or controls. Existing pause/static fallbacks remain. Other homepage components, descriptions/locales, backend billing sources and the embedded production index are byte-unchanged, verified by before/after checksums. Changes are limited to the trust component, its tests, the wrapper's trust expectations and documentation.
- Final homepage suite: 142/142 across 13 files, including 12 trust tests. New regressions cover shared cable endpoints, single-subpath signal geometry, portrait routing, identical source/receipt fingerprints, fixed slot bounds, hidden future rows, row-height feed increments, shared row ink/price clipping and delivery-before-print-before-settlement timing. Full suite: 1042 pass, the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription dates), 150 files. Full lint: zero errors / 12 existing unrelated warnings. Initial concurrent typecheck exited 143 without a diagnostic; normal standalone retry passed. Final isolated build passed in 39.98s with the existing chunk warning. Diff check passes.
- Inspected offline Vue SSR/librsvg compositions for partial printing, two printed rows, complete portrait and dark theme. Samples also cover the header, connected delivery, all three feed steps, total, fade and reset. They use representative static phase positions, not browser CSS playback/FPS or live page layout/HTTP verification. Snapshots, samplers, logs, publisher and verifier: `/var/tmp/sub2api-trust-print-artwork/`. No restricted-browser/network workaround attempted.
- Updated only the existing static preview at port 4178 from `/var/tmp/sub2api-home-trust-print-dist`: entry `/assets/index-6Q9HH-1v.js`, HomeView `HomeView-BCak4Yvt.js` / `HomeView-CErXt_TS.css`. Backup `/var/tmp/sub2api-home-pre-trust-print-backup`. All 160 published assets, local imports, disk index, cached/current JS aliases and cached CSS verified offline. Startup `index-CXxW4uoe.js` and former `index-hvoPdePO.js` bridge directly to the new entry. No server restart or production deployment.
- To preserve publication headroom, archived only six documented, non-served historical preview backups: all-descriptions-right, astra-crisp, astra-edit, astra-feature, model-vector-rotation and celestial-final. All 28 payloads are preserved under `/var/tmp/sub2api-home-trust-print-backup-archive/`; original paths remain byte-verified symlinks, with a SHA-256 manifest. No discarded backups, active-asset relocation or general cleanup. `/tmp` remains near capacity (~9.2 MB free); future artifacts/builds should stay in `/var/tmp` and publications must check capacity.
- No production lifecycle action, proxy/container/database change, commit or push. Browser playback and live HTTP health remain unverified.

### 2026-09-22 — One request per usage row, with a central billing process

- Corrected the scene's data model after the user's clarification. The left list now contains three independent requests; each has one corresponding central calculation and one printed usage record. Reusable, instance-scoped request fingerprints and model marks match across all three stages. Input/cache/output categories exist only inside the active request's calculation, never as top-level requests or receipt entries.
- Read the existing `UsageView.vue` cost/token presentation, `UsageLog` fields, `OpenAIGatewayService.RecordUsage` cached-input normalization and billing-service cost aggregation. The central SVG now illustrates model unit-price selection → ordinary input/cache-read/output quantities → per-category costs → `total_cost` → one effective `rate_multiplier` → `actual_cost`. Cached pieces physically separate inside each request, with no duplicate in ordinary input. Graphical term widths sum to the subtotal and the adjusted width is copied unchanged into that request's receipt row. These authored values are drawing units, not real prices, usage or rates; the scene does not fetch actual requests or private usage logs. This is a token/cache-read illustration, not an exhaustive simulation of image/per-request/service-tier/cache-creation pricing.
- Two grounded routes replace the direct request-to-printer connection: requests enter the calculator's model-pricing inlet, and completed charges leave its output for the printer. The outgoing signal follows completion of the per-request multiplier; printing follows signal arrival. One 16s CSS clock processes requests at 4s offsets without overlapping calculation windows. The last calculation holds during final reconciliation. The existing unscaled paper feed, fixed slot, row-by-row ink and invisible reset remain. Removed the receipt-wide multiplier and combined monetary settlement; the footer only reconciles records and does not imply an additional charge.
- The scene has 296 simple SVG elements and no filters, raster assets, fonts, timers, JS frame loop, controls, dependencies or account calls. Desktop retains 1200×520 with three columns. Portrait retains 600×720, with history/calculation side by side above the full-size receipt and an external right-hand cable. Existing native visibility suspension/static fallback remain. Only this component, trust tests, wrapper expectations and each locale's screen-reader billing summary changed; original descriptions, other scenes, actual usage UI, billing code and embedded production index are checksum-unchanged.
- Homepage suite: 145/145 across 13 files, including 15 trust tests for one-to-one identity/model mapping, per-request fields, cache separation, category sum/rate/result consistency, no second receipt adjustment, route endpoints, portrait bounds, fixed-slot feeding, synchronized sequence, scoped resources and accessible summaries. Full frontend suite: 1045 pass / five established unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription date offsets), 150 files. Full lint: zero errors / 12 existing unrelated warnings. Typecheck passes. Isolated build passes in 30.23s with the existing chunk warning. Diff check passes.
- Inspected offline Vue SSR/librsvg samples for cache partition, calculated charge, partial printing, complete desktop/mobile and dark theme. Additional samples cover request arrival, unit pricing, multiplier, outgoing charge, each printed row and reset. These representative static compositions are not browser animation/FPS, page-layout or live HTTP verification. Artifacts, snapshots, samplers, logs and publishing scripts: `/var/tmp/sub2api-trust-per-request-artwork/`. No restricted-browser/network workaround attempted.
- Refreshed only the existing isolated preview at port 4178 from `/var/tmp/sub2api-home-trust-per-request-dist`: entry `/assets/index-BsoG7QvM.js`; HomeView `HomeView-26Ecrbwm.js` / `HomeView-C6MOtsZh.css`. Backup `/var/tmp/sub2api-home-pre-trust-per-request-backup`. Verified all 160 asset bytes, local imports, disk index, cached/current JS aliases and cached CSS. Startup `index-CXxW4uoe.js` and former `index-6Q9HH-1v.js` bridge directly to the new entry. No preview service restart or production deployment.
- Publication capacity was preserved by archiving only 17 documented, non-served historical preview backup folders, from `policy-panels` through `model-groups` in the explicit helper list. All 51 files are retained under `/var/tmp/sub2api-home-trust-per-request-backup-archive/`, with original-path symlinks and a fully rechecked SHA-256 manifest. No discarded backup, active preview asset relocation or general cleanup. `/tmp` remains near capacity (~9 MB free); keep future artifacts in `/var/tmp` and check publication headroom first.
- No production lifecycle action, proxy/container/database change, commit or push. Browser playback and HTTP health remain unverified.

### 2026-09-22 — Equal feature-description footer spacing

- Found that intelligence used a full-bleed frame with its own footer padding, while speed/trust used the generic frame's bottom padding plus an additional `12px` margin and separate top padding. The two box models produced different divider-to-copy and copy-to-edge spacing even though all three descriptions share the same markup.
- Normalized the vertical rhythm at every breakpoint: no extra footer margin, matching divider-to-text padding, and matching bottom gutters for the generic and full-bleed intelligence frames. The art geometry, descriptions, right alignment, animation timing and scene implementations are unchanged. Added a source regression covering base, desktop and mobile spacing.
- All 145 homepage tests passed before the final desktop-low-height normalization; the final focused run passes 18/18 tests across the carousel and wrapper. Scoped lint, typecheck and isolated Vite build pass; the existing large-chunk warning remains. Build output: `/var/tmp/sub2api-home-footer-spacing-v2-dist`. Browser layout/playback and live HTTP health remain unverified.
- Refreshed only the existing non-production preview styling at port 4178. The preview remains on entry `/assets/index-BYZz8xLM.js`; its already-loaded scoped HomeView CSS was atomically given the same final desktop rule, with backup `/var/tmp/sub2api-home-pre-footer-spacing-v2-backup`. No server restart or production action occurred.
- To retain publication headroom, 45 files from 15 documented, non-served historical preview backups were preserved under `/var/tmp/sub2api-home-footer-spacing-backup-archive/` and `/var/tmp/sub2api-home-footer-spacing-backup-archive-2/`. Original paths remain hash-verified symlinks; no backup was discarded and no active preview asset was relocated.

### 2026-09-22 — First-tab footer is the shared layout

- Corrected the normalization direction after the owner clarified that the original intelligence-tab footer was the desired reference. The single shared `.feature-copy` rule now owns all padding and the divider for intelligence, speed and trust. Intelligence has no footer-specific override. Speed/trust only cancel their animation canvas's outer inset so the shared footer reaches the same edges; their scene artwork keeps its existing safe inset.
- Restored the first tab's exact responsive footer padding: `16px 32px 24px` at base, `12px 28px` on desktop, and `14px 18px 20px` on mobile. Added a regression that rejects any intelligence-specific footer declaration and checks the shared rules. Focused carousel/wrapper tests pass 18/18; scoped lint, typecheck and isolated Vite build pass. Build: `/var/tmp/sub2api-home-first-footer-dist`.
- Updated only the existing preview's scoped HomeView stylesheet atomically, preserving entry `/assets/index-BYZz8xLM.js`; backup: `/var/tmp/sub2api-home-pre-first-footer-backup`. No preview restart, production action, browser/HTTP verification, commit or push.

### 2026-09-22 — Compact ChatGPT logo clear of model cards on short screens

- Root cause: `.model-brand` scaled around the center of its original SVG-plus-wordmark box. Its nominal `top: 12px` in the fitted desktop layout therefore acquired about 85px of visual downward offset at scale .38, overlapping the 124–164px reserved header space. Changed only its transform origin to `top center`. The compact box now starts at the configured inset. Scale-1 intro/manual stages remain centered; sizes, transitions, rotation, model cards and feature footers are unchanged.
- Added regressions for the shared static/keyframe anchor and conservative full-box clearance at desktop heights 600, 699, 700, 720, 768, 900 and 1080px. These are CSS/geometry contracts, not browser layout measurements. The template/script and all other component CSS match the before snapshot. Other homepage scenes, carousel, HomeView, locales and embedded production index remain checksum-identical.
- Focused model tests 9/9; homepage suite 148/148 across 13 files. Full frontend suite 1048 pass with the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription date offsets). Full lint has zero errors and 12 existing unrelated warnings. Typecheck passes; isolated build passes in 37.38s with the existing chunk warning. Diff check passes. Logs, before snapshots and build are under `/var/tmp/sub2api-model-logo-anchor/`.
- Published only the matching model-brand CSS declaration to the existing port-4178 static preview, atomically and with backup `/var/tmp/sub2api-model-logo-anchor/preview-before.css`. The rule matches the isolated build after normalizing to the existing scope ID. Entry remains `index-BYZz8xLM.js`; HomeView JS and all remaining CSS bytes are unchanged. Publisher, manifest and log are in the artifact directory. No new preview bundle copy, backup relocation, service restart, production action, commit or push. Browser playback/layout and HTTP health remain unverified in this restricted environment.

### 2026-09-22 — Entire feature-tab layout shared from the first tab

- The previous change shared only footer padding; speed/trust still had inset frames, different canvas minimums and negative-margin compensation. Promoted the original intelligence frame/art declarations to the common selectors: zero frame padding, page-color background, full-width canvas in the first grid row and a separate shared description row. Base/mobile/fitted-desktop canvas minimums are 320/400/200px, matching the first tab's existing rules. Removed every feature-specific wrapper selector and all compensating negative margins. The first-tab layout rules are the reference, not a blend of the three layouts.
- Template, selection logic and animations are unchanged. All three SVGs keep their original desktop/portrait geometry, centered aspect-ratio handling and visibility suspension. Original descriptions, prior model-logo positioning fix, HomeView, other scene components, locales and embedded production index are checksum-unchanged.
- Focused carousel/wrapper tests 19/19; homepage suite 149/149. Regressions enforce common frame/art/footer rules at every breakpoint, reject per-feature CSS and negative margins, and verify the identical two-child frame structure with matching canvas viewBox/aspect handling. Full frontend suite 1049 pass with the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint has zero errors / 12 existing warnings; typecheck and isolated build pass (35.52s, existing chunk warning). Diff check passes.
- Artifacts, before snapshots, logs and isolated build: `/var/tmp/sub2api-unified-feature-frame/`. Published only the carousel's compiled scoped CSS block, normalized to the existing scope ID, via an atomic preview-file replacement. Backup `preview-before.css`, publisher `publish-preview.cjs`, manifest and log are in that directory. Preview entry remains `index-BYZz8xLM.js`; JS, HTML, all other CSS and the preceding model-logo fix are preserved. No service restart, production action, broad cleanup, commit or push. Browser geometry/playback and HTTP health remain unverified; unit checks are not browser layout measurements.

### 2026-09-22 — Right-corner captions, automatic feature tour and 1.5s end holds

- Moved the three numbered intelligence captions to the lower right with right-aligned text. Their existing timing does not overlap the ASTRA finale, which uses the same corner. Shared frame/art/footer layout and original descriptions remain unchanged.
- Added an initial Intelligence → Speed → Trust loop. A single invisible, active-frame-only native CSS phase clock reaches each completed-result plateau (12.6 / 14.72 / 14.88 seconds), then holds the artwork and captions for 1.5 additional seconds before switching. No JavaScript interval/timeout or frame loop is used. The phase clock pauses with offscreen/document-hidden state; only the artwork pauses during the extra hold. Unrelated SVG animation events cannot advance the carousel.
- Manual selector clicks, including the already selected tab, and the existing keyboard selection cancel auto-rotation for the mounted carousel. Individual scenes keep looping: hold the completed result, resume the original fade/reset tail, restart with a fresh scene key. All selected scenes restart from the beginning; intelligence captions share its restart key. The scene source timelines and geometry are unchanged, so manual cycles become 15.5s for intelligence and 17.5s for speed/trust. No new visible copy or controls; reduced-motion/manual browsing remain supported.
- Focused carousel tests 19/19; homepage suite 155/155. Regressions cover a complete tour, end-hold durations, fresh restarts, same-tab/manual/key cancellation, continued manual scene loops, selecting during a hold, unrelated events and visibility suspension. Full frontend suite: 1055 pass, five established unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint: zero errors / 12 existing warnings. Typecheck and isolated build pass (34.42s, existing chunk warning); diff check passes. All scene components, model-logo fix, HomeView, locales and embedded production index remain checksum-identical.
- Artifacts and build: `/var/tmp/sub2api-feature-autoplay/`. Refreshed only the existing static preview to `/assets/index-Dglc780U.js`, HomeView `HomeView-CYM84BTj.js` / `HomeView-CGgbxlLz.css`. Startup `index-CXxW4uoe.js` and previous `index-BYZz8xLM.js` bridge to the new entry. Backup: `preview-backup/` in the artifact directory, including the previously patched HomeView CSS. All 160 asset bytes, imports, index, entry bridges and cached CSS verified offline. Publication accounted for filesystem allocation rounding and reserved 2 MiB plus the largest staging file; ~5.3 MiB remains free in `/tmp`. No cleanup, backup loss, service restart, production operation, commit or push. Browser playback/geometry and live HTTP health remain unverified; native-clock behavior is covered by state/CSS contracts, not browser timing measurements.

### 2026-09-22 — Overlapping feature transitions instead of an immediate timeline handoff

- Added a 900ms entrance phase to the existing native CSS clock for both automatic and manual tab changes. The outgoing scene stays mounted and paused while fading out; the incoming artwork/captions wait until their entrance finishes before starting the original timeline. The first load and same-scene loop resets do not pay an extra entrance delay. Clicking the current selector now only cancels autoplay, without visibly restarting it. Reduced motion skips the entrance; 1.5s final holds and manual-loop behavior remain intact.
- The shared stage now owns the stationary background. Frames crossfade with an explicit stacking order and delayed visibility hiding; SVG canvases are transparent, with a subtle 12px / .985-scale entrance and departure. Native entrance keyframes handle fresh SVG nodes that lack a prior computed transform. The frame/divider no longer translates. Description text has a short stagger and the selected-tab accent fades. No filters, layout-dimension animation, new dependencies or JS frame/timer loops. Offscreen/background pause also covers the entrance clock and keyframes.
- Carousel tests 22/22; final homepage suite 158/158. Added regressions for retained outgoing DOM, entrance-before-play sequencing, current-tab non-reset, fast/latest selection, hidden-page entrance pause, reduced-motion skipping and shared stationary geometry. Full frontend suite 1058 pass with the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover, two subscription dates). Full lint zero errors / 12 existing warnings; typecheck passes. After the final CSS-only native-entry refinement, homepage tests, scoped lint and isolated build pass again (31.65s, existing chunk warning). Diff check passes. Scene sources, shared scene wrapper, model-logo fix, HomeView, locales and embedded production index are checksum-unchanged.
- Artifacts: `/var/tmp/sub2api-feature-transitions/`, including snapshots, logs, publishers, manifests and `final-dist/`. Existing static preview entry is `/assets/index-DRnfBfPa.js`, with `HomeView-CyQQf_Mf.js` / `HomeView-w7BTSrGT.css`. The final carousel CSS block was atomically replaced from the final build, normalized to the existing scope ID; JS/HTML and unrelated CSS remain unchanged. Full publication backup is `preview-backup/`; final CSS backup is `preview-before.css`. Verified all 160 assets, entry bridges and final patched CSS separately using both manifests. No service restart or production action.
- To provide copy headroom, preserved two historical non-served PNG screenshots (`sub2api-slider-final-welcome.png`, `sub2api-feature-carousel-final-desktop.png`, both from documented screenshot sets) under `historical-screenshots/`, retaining original `/tmp` paths as SHA-256-verified symlinks. No discarded evidence, active-asset relocation or broad cleanup. `/tmp` now has about 3.5 MiB free; future builds/logs must stay in `/var/tmp` and publication must check capacity. Browser playback/geometry and HTTP health remain unverified; no restricted-browser/network workaround, commit or push.

### 2026-09-22 — Balance card highlights availability, not the Astra multiplier

- Replaced the balance card's hard-coded `0.4–0.5 ×` metric with localized `随时使用` / `Use anytime`. Removed its Astra-multiplier caption and entire group-rate fact. The card retains only top-up and usage facts; the subscription metric, caption and three facts are unchanged. Removed the now-unused balance unit/caption/pricing keys in both locales and added one availability key. Existing card CSS is unchanged. No actual rates, backend billing, usage UI, other homepage copy or animations were changed.
- Homepage suite 160/160, including bilingual regressions for the availability metric, absence of multiplier/unit/caption, exactly two balance facts and unchanged subscription information. Full frontend suite 1060 pass with the same five unrelated failures (persisted page size, OAuth affiliate parameter, tooltip hover and two subscription dates). Full lint zero errors / 12 existing warnings; typecheck and isolated build pass (32.11s, existing chunk warning). Diff check passes. Carousel/scene sources, model-logo fix, HomeView, billing service, user usage UI and embedded production index remain checksum-identical.
- Artifacts, locale-only diffs, logs and build: `/var/tmp/sub2api-balance-anytime/`. Existing static preview refreshed to `/assets/index-BNWh8Z9n.js`, HomeView `HomeView-8gdGS-51.js` / `HomeView-C-B3X0-i.css`. Backup `preview-backup/` includes the prior patched stylesheet. Startup `index-CXxW4uoe.js` and previous `index-DRnfBfPa.js` bridge to the new entry. All 160 assets, imports, index, bridges and cached CSS verified offline, including the prior native entrance keyframes and top-anchored model logo.
- To retain safe copy headroom, preserved four historical non-served PNG screenshots from documented screenshot sets (`sub2api-continuous-dark.png`, `sub2api-fluid-glass.png`, `sub2api-fluid-dark-glass.png`, `sub2api-stable-glass-shadow-dark.png`) under `historical-screenshots/`, retaining all original paths as hash-verified symlinks. No backup/evidence loss, active-asset relocation or general cleanup. About 3.4 MiB remains free in `/tmp`; keep future artifacts in `/var/tmp` and check publication capacity. No service restart, production action, browser/HTTP verification, commit or push.

### 2026-09-22 — Equal-width client links

- Changed only the client-button group in HomeView to two equal grid columns, sized to content and capped at the container width. Both Codex and Pi center their icon/label; links, icons, colors, spacing and existing compact sizing remain unchanged. No fixed viewport-specific button widths or script changes.
- Homepage tests 161/161, including a CSS-contract regression for equal columns and centered content; scoped ESLint, typecheck, isolated build (35.01s, existing chunk warning) and diff check pass. Full unrelated frontend suite not rerun for this CSS-only change. Home components, locales and embedded production index are checksum-unchanged. No browser geometry/playback or HTTP verification.
- Artifacts: `/var/tmp/sub2api-client-buttons/`. Updated only the two base client-button rules in preview `HomeView-C-B3X0-i.css`, using compiled build declarations normalized to the existing scope. The entry remains `/assets/index-BNWh8Z9n.js`; HTML, JavaScript, all other CSS and the other 159 build assets remain identical. Backup `preview-before.css`, exact changes/hashes in `preview-css-manifest.json`. No service/process action, production change, screenshot relocation, cleanup, commit or push.

### 2026-09-22 — Source publication: cafecode-v0.0.78

- User explicitly authorized commit, push and tag. The next unoccupied release tag is `cafecode-v0.0.78`, following remote `cafecode-v0.0.77`; branch `custom-prod` starts at `fee9f04bd8df3e3e331e95a7a3c1121ab6eb7b4e`. Existing tag workflow builds/publishes an image only; no production deployment, lifecycle command, proxy/configuration change or database action is part of this publication.
- Staged only the homepage's 51 files: HomeView, native SVG components/helpers/assets/licenses, generators, homepage tests, D3 dependencies, homepage locale additions and this task's documents. Used partial locale staging to leave ingress-host/client-IP labels with the unrelated usage-record work. Backend changes/migrations, admin/user usage UI, unrelated tasks/journals, concept mockups, root artwork and local operational files are excluded and preserved. Removed one trailing space from the vendored OFL text without changing its wording.
- Exported staged tree `b1fd14a1de4edda67db9b3105e16bbaf61b777fa` to an isolated snapshot under `/var/tmp/sub2api-home-release-078/`, reusing installed dependency packages but none of the uncommitted source. All 161 homepage tests pass; the exact CI critical test command passes 107/107. Full frontend suite: 1055 pass, five fail across the same four baseline test files. Reran those files from parent HEAD in a separate baseline snapshot and reproduced exactly the same five failures (persisted page size, OAuth affiliate query, tooltip hover and two subscription dates). No unrelated fixes were included.
- Full lint: zero errors / 12 pre-existing warnings. Typecheck passes. Isolated Vite build passes in 35.18s with the existing chunk-size warning, output outside the production embedded assets. Staged diff check passes. Only this task's release documentation was updated after the code snapshot validation. All previous working source bytes were preserved except the stated license whitespace and task metadata; unrelated locale hunks remain unstaged. Logs, staged allowlist, snapshots and preservation hashes are retained in the artifact directory. No additional browser/HTTP preview verification was performed for this source-only publication.

### 2026-09-22 — Symmetric billing cards, completed-result holds and smaller header name

- Removed subscription pricing facts in both locales; both plans now share exactly `access` and `period`. Replaced dissimilar `1 / month` and oversized `随时使用` highlights with paired `按月 / 订阅` and `按需 / 充值` (English: `Monthly / plan`, `Flexible / top-ups`). Both cards render a highlight, unit and caption through the same template, preserving the one-month limit and anytime-availability meanings. Shared reduced typography, flex columns and two equal fact tracks align the title/highlight/caption/dividers without fixed card heights. No actual billing/pricing behavior changed.
- Fixed the intelligence hold boundary: the second signal previously finished at 96%, but the carousel paused it at 90%. It now holds at 97% (13.58s), after the signal reaches the endpoint and exits; all completed-result geometry and the ASTRA signature stay fully visible through 98%. Retained the extra 1.5s hold and 900ms crossfade. Speed's independent globe/world-traffic loops have an explicit ambient marker so they keep moving during its completed-client hold, while inactive/offscreen/background states still pause them. The trust scene already completes its receipt/check before the hold and is unchanged. Header name scale reduced from 1 to .9, and .875 to .79 on mobile; letter geometry and header height are unchanged.
- Homepage suite 164/164; scoped ESLint, typecheck, isolated build (34.84s, existing chunk warning), and diff check pass. No rerun of the unrelated full frontend suite for these follow-ups; its five baseline failures were independently reproduced during the preceding release. HomeView (including equal client widths), model showcase, trust artwork and embedded production index are checksum-unchanged. Unrelated working changes/locale labels remain intact; no new commit, tag or push.
- With the environment's browser/network restriction now lifted, real Chromium verified 12 Chinese/English layouts at widths 1440, 1366, 1024, 768, 390 and 320: equal card/title/highlight/caption/fact-row positions on side-by-side layouts, identical headline font size, two facts each, and no overflow or runtime errors. Light/dark desktop screenshots and separately scrolled mobile card screenshots were captured; whole-section screenshots beyond the mobile scrollport are not evidence of visible offscreen content. The preview responds HTTP 200.
- Chromium sought the real CSS timelines close to their endings, then let their native clocks finish naturally. Verified moving intelligence highlight before arrival, zero signal opacity and complete result/check/signature during the hold, continuing global traffic throughout the speed hold, completed trust receipt/check, automatic crossfades/wraparound, manual cancellation, offscreen suspension, and actual .9 header scaling. This is targeted browser state/motion verification, not a full-cycle performance/FPS benchmark.
- Artifacts/logs/build/screenshots/scripts: `/var/tmp/sub2api-billing-pair/`. Preview entry `/assets/index-BVVYdFUk.js`, HomeView `HomeView-DpZF4Jvh.js` / `HomeView-lKP4iujE.css`; all 160 build assets, imports, HTML and entry/CSS bridges verified byte-for-byte. Backup `preview-backup/` includes the preceding two-rule client-button CSS patch. The first publisher run finished copying then hit an overly strict custom-property whitespace assertion; corrected it and revalidated the already published files without replacing the backup or publishing twice. `preview-manifest.json` records the final verified state.
- To maintain safe `/tmp` copy headroom, preserved nine documented non-served historical PNGs under `historical-screenshots/` with original-path, hash-verified symlinks: four `roomy-scenes-final` samples, three `unified-features` samples, `continuous-everyday` and `feature-carousel-final-dark`. No evidence loss, active-asset relocation or general cleanup; about 3.3 MiB remains in `/tmp`. No service restart, production action, database/configuration change or new release. Existing `cafecode-v0.0.78` remains untouched and does not include these follow-up edits.

### 2026-09-22 — Follow-up source publication: cafecode-v0.0.79

- User explicitly requested commit, tag and push for the preceding billing/animation/header refinements. Remote `custom-prod` and `cafecode-v0.0.78` both point to `8eed4a64f74ebe3d94bacd5df97bc7c06b029662`; `cafecode-v0.0.79` is the next unoccupied stable release tag. The previous release is not moved or amended. This is source/image-artifact publication only, not deployment.
- The release includes 14 files: five homepage components, three component tests, HomeView tests, homepage README, the two billing locale hunks, and task metadata/verification. Partial staging preserves unrelated ingress-host/client-IP translations in the working tree. Backend code/migrations, usage interfaces/types, dependencies, CI workflows, unrelated tasks/journals and operational assets remain excluded and unchanged.
- Exported staged tree `fb2e4d8c4a0c9307aa1d64b53bb74620fb471eb8` to `/var/tmp/sub2api-home-release-079/snapshot/`. With the same installed dependencies but no unstaged source, homepage tests pass 164/164, the exact CI critical command passes 107/107, full lint has zero errors / 12 existing warnings, typecheck passes and isolated build passes in 31.84s with the existing chunk-size warning. The earlier browser checks apply to the unchanged homepage implementation. The unrelated full frontend suite was not repeated; its five known baseline failures were reproduced during v0.0.78 preparation.
- Only task release metadata/verification changes after snapshot testing. Staged diff/allowlist, backend/CI subtree equality, partial locales and preservation hashes are checked before committing. Artifacts include staged source/tree, original working-file hashes, logs and isolated build under `/var/tmp/sub2api-home-release-079/`. No production service, container, database, proxy, preview process or preview asset is modified by this release operation.
